# UX Playbook（交互单一事实来源）

> 这是"用户想要的 UX"的可执行定义。落地 UI 时的**唯一交互依据**，不临场发明。
> 与用户/PM 确认后定稿；变更须经 PM 确认。rd-fe 交付前按此核对。

## 页面四态（每个可加载模块必须覆盖）

| 状态 | 表现 | 位置 |
| :--- | :--- | :--- |
| 加载 | 骨架屏（列表/卡片）或按钮 loading；首次整页加载用骨架 | 内容区 |
| 空态 | 图标 + 主文案 + 行动按钮（如"去创建"） | 内容区居中 |
| 错误 | 行内错误或全局错误条 + 重试按钮；文案含可操作建议 | 就近展示 |
| 成功 | 轻量 Toast（默认 2s），不打断 | 顶部居中/右下 |

## 反馈模式

- **危险操作**（删除/清空/退出）：二次确认 Dialog，按钮「确认删除 / 取消」。
- **提交**：按钮 loading；成功后跳转或 Toast；失败就地显示错误。
- **表单校验**：失焦即时校验，错误就地显示；提交时全量校验；提交中防重复。
- **异步刷新**：静默刷新失败不弹框，用角标/状态条提示可重试。

## 导航与键盘

- 列表支持键盘上下选择；Dialog 支持 Esc 关闭 + Focus 陷阱。
- 移动端底部操作条；桌面侧栏导航；详情页返回保留滚动位置。
- 长列表虚拟滚动；加载更多用"加载中"占位而非跳动。

## 文案

- 空态/错误用**主动语态 + 下一步**（"还没有任务，点这里新建"），禁被动"发生错误"。
- 按钮动词化（"保存""删除"）；禁用灰色 + 说明原因。

## 一致性

- 同一含义只用一种反馈方式（如删除永远是确认 Dialog，不混 Toast）。
- 新交互进本 Playbook，不进代码注释。
