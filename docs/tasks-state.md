# 任务状态（PM 维护，运行时事实）

> 每次派发 / 回执 / 验收 / 抢占后更新本文件；PM 会话重开（`ensure --force`）后**先读本文件重建状态**再继续调度。
> 归档后的历史见 `docs/prds/` 与 `docs/adr/`；本文件只记**运行时**任务状态，保持精简。

- **当前批次**：TASK-13 导出任务文本对话框优化（可编辑 + 子任务数据丰富）
- **PRD**：`docs/prds/2026-09-21-task-export-editable-enriched.md`
- **阶段**：**已交付（技术验收闭环）**，等待用户终签（4 项视觉人眼确认）
- **上一阶段检查点**：`e4e1064f`（实现）/ `4cc0e34b`（测试）/ `4d7fd0d9`（文档）—— 已验收，可回滚
- **调度授权**：2026-09-21 用户授权「持续推进到落地」
- **需求池（撤回/搁置）**：TASK-14「日历视图刷新功能」——用户 2026-09-21 **撤回**，待其自行测试一段时间后再提（**未澄清、未开工、无 PRD**）。
- **当前批次（TASK-15）**：日历视图展示顺序——撤销隐藏排序 + 二次默认排序按名称；PRD `docs/prds/2026-09-21-calendar-display-order.md`；开工确认：用户 2026-09-21 拍板「按推荐（方案 B）」，并授权继续推进。
- **TASK-15 范围（3 处撤销 + 1 处保留）**：①`monthly-layout.ts` 删 `colEnd desc` 二次键（保留 `colStart asc`）②日内抽屉 `getDayTasks` 撤销「开始时间→创建时间」③未安排列表 `unscheduledTasks` 撤销 `createdAt desc`；**保留** 服务端 `sort:{id asc}`（分页机制）。
- **发版策略**：用户指示「先不发版，新需求做完统一发一次版」——本批次 7 个提交与后续提交均留在本地 `feat/ocdev`，**push 与 tag 一并推迟到统一发版**。
- **当前批次（TASK-16）**：日历界面**日视图**（横坐标=时间，48 个 30 分钟列；整点有文本 / 半点无文本；分钟级连续定位；轨道+`+N`；跨日裁剪+续接；全天行；时间线；快速新建/拖拽改时间/拉伸改时长）。**PRD 已定稿待开工确认**：`docs/prds/2026-09-21-calendar-day-view.md`；澄清完成 Q1–Q6（用户答：Q1=A/B、Q2=B、Q3=a、Q4=a+a、**Q5=全做**、Q6=按推荐），Q2 子问与 Q7 未答 → PRD §0 已声明假设 A1/A2。任务单 T48（arch）/ T49（qa 基线）/ T50（rd-fe 只读渲染）/ T51（rd-fe 交互）/ T52（qa 独立验收）**均未派发**。

- **治理授权（2026-09-21 用户授权）**：①PM **可直接提交自有记账文档**（PRD 记账段 / `docs/prds/README.md` 索引 / 本文件），无需再派微单；**代码与实现产物（含测试）仍一律由实现方提交**。②已补 `AGENTS.md` nao 舰队接入区块（`44f6280f`，只增不改，原文逐字节未动，115 行）。
- **治理待办**：`.agents/**`（nao 舰队接入在制品，7 M + 6 未跟踪目录/文件）、`.codegraph/**`、`.pi/**` 仍未提交——属独立治理线，待用户决定归属与提交时机。
- **更新**：2026-09-21

## TASK-15 运行时（当前批次）

| 任务编号 | 目标会话 | 角色 | 概要                                                          | 状态                                                                                                                                                                                                                                  |
| :------- | :------- | :--- | :------------------------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T44      | rd-fe    | 前端 | 撤销 3 处隐藏排序 + 10 例单测                                 | ✅ 已回执并验收（PM 复跑全仓 94 文件 / **823 例全绿**、`guard:ddd` OK、移动端零改动、`list-query.ts` 未动；读码确认 `.sort((a,b)=>a.colStart-b.colStart)` 无 `colEnd`、`getDayTasks`→`sortedTasks`、`unscheduledTasks` 由组装点派生） |
| T45      | qa       | 测试 | 独立复核四处顺序一致性 + 判别力论证 + 补抽屉/未安排组合式验证 | ✅ 已回执并验收（PM 复跑 **96 文件 / 832 例全绿**、`vp check` 全绿、`guard:ddd` OK、双端 build exit 0；**qa 查出 T44 AC1 用例未绑定集成**，已由新增 `calendar-order-source.test.ts` 闭环）                                            |
| T46      | rd-fe    | 前端 | TASK-15 提交（精确 pathspec，不夹带 .agents/.codegraph/.pi）  | ⏳ 已派发                                                                                                                                                                                                                             |

## TASK-16 运行时（当前批次）

| 任务编号 | 目标会话      | 角色 | 概要                                                                                                | 状态                                                                                                                                                                                       |
| :------- | :------------ | :--- | :-------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T48      | arch-designer | 架构 | 列模型参数化 / 打包抽取复用 / 分钟吸附与跨日领域边界（A4/A5 裁决）                                  | ⏳ 已派发                                                                                                                                                                                  |
| T49      | qa            | 测试 | 用例先行（5 文件 39 例：几何/lane-packing/snap/build-day-grid/日视图 DOM）                          | ✅ 已回执并验收（全红属预期；零改动复用既有用例；已按 §5.7 冻结口径迁移到 `daily/` 与 `calendar/snap.ts`）                                                                                 |
| T50      | rd-fe         | 前端 | 只读渲染（`lane-packing`/`snap`/`segmentStyleInColumns`/`buildDayGrid`/`daily/index.vue`/三态接入） | ✅ 已回执并验收（PM 复跑全仓 **101 文件 / 872 例全绿**、`vp check` 全绿、`guard:ddd` OK、移动端零改动、T49/T54 测试文件 mtime 未被触碰；C1–C14 自检齐备）                                  |
| T51      | rd-fe         | 前端 | 交互实现（新建 floor / 拖拽·拉伸 round / 撤销并入 / 失败回退 / C7 不刷新）                          | ⏳ 已派发                                                                                                                                                                                  |
| T54      | qa            | 测试 | 修正 T49 两条断言 + 新增复用用例                                                                    | ✅ 已回执并验收                                                                                                                                                                            |
| T55      | qa            | 测试 | 交互组件级行为用例（8 例，全红预期）                                                                | ✅ 已回执并验收（仅新增 1 文件；未触碰 T49/T54；5 项待冻结项已裁决并写入 PRD §5.7）                                                                                                        |
| T56      | rd-fe         | 前端 | 根治 `.agents/**` × 格式化冲突                                                                      | ✅ 已回执并验收（`28992363`，2 文件；**PM 独立复现容错**：26 文件转 CRLF + roles 4/8 → 解析成功/0 ERR/0 frontmatter 失败；`vp check --fix .agents` → NO FILE CHANGED；`vp check` 全绿 1279 | 1100） |
| T57      | rd-fe         | 前端 | CodeGraph 索引警告不再计入 check 退出码（误停派发）                                                 | ⏳ 已派发                                                                                                                                                                                  |
| T52      | qa            | 测试 | 独立验收（含双端 build / 红线 / 拖拽性能）                                                          | 排队（等 T51）                                                                                                                                                                             |

## 待派发队列

| 任务编号 | 目标会话 | 角色 | 概要 | 排队原因 |
| :------- | :------- | :--- | :--- | :------- |

## 进行中

| 任务编号 | 目标会话 | 角色 | 概要 | 派发时间 | 对应 AC |
| :------- | :------- | :--- | :--- | :------- | :------ |
| —        | —        | —    | —    | —        | —       |

## 已回执待验收

| 任务编号 | 回执摘要（≤150字） | 详情路径 | 待办 |
| :------- | :----------------- | :------- | :--- |
| —        | —                  | —        | —    |

## 挂起（被抢占 / 降级）

| 任务编号 | 挂起原因 | 恢复方式 |
| :------- | :------- | :------- |
| —        | —        | —        |

## 已验收 / 已归档

| 任务编号 | 结论                                                                                                                                                                                                                                             | 归档路径                                                                                 |
| :------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------- |
| T32      | 验收通过（ADR 落盘 / 整体替换论断 / 仓储排序口径属实）；PM 拍板 D1=A′、D2=进入公共导出面，C1–C3 写入 PRD §9                                                                                                                                      | `docs/adr/2026-09-21-task-export-readonly-checkitem-fetch.md`                            |
| T36      | 验收通过（ADR 状态 `✅ 已采纳`；README `:22`/`:42`/`:43` 同步；§7 拍板留痕齐备）                                                                                                                                                                 | 同上                                                                                     |
| T33      | 验收通过（PM 复跑：30 例 = 17 红 / 13 绿，与回执逐项一致；`__tests__` 外无产品代码改动）                                                                                                                                                         | `packages/presentation/task/components/task-details/__tests__/`                          |
| T34      | 验收通过（PM 复跑 795 例全绿 / `vp check` pass / `guard:ddd` OK / 移动端零改动 / T33 断言未被触碰）。rd-fe **越界**实现原 T35 范围 → 裁决不回退但取消 T35                                                                                        | commit `e4e1064f`                                                                        |
| T35      | **取消**（并入 T34）：无剩余实现范围，独立验收改由 T37 承担                                                                                                                                                                                      | —                                                                                        |
| T37      | 验收通过（AC1–AC5 五覆盖 + 双端 build exit 0 + 移动端零改动 + 断言未削弱复核；新增 7 例）                                                                                                                                                        | `packages/presentation/task/components/task-details/__tests__/export-acceptance.test.ts` |
| T38      | 验收通过（3 提交 pathspec 精确、无 `.agents/**` 夹带、未 push；§5.2 块未被钩子改写）                                                                                                                                                             | `e4e1064f` / `4cc0e34b` / `4d7fd0d9`                                                     |
| T39      | 验收通过（变更 1 基线重写：46 绿 / 14 红，红项分布与规格逐项一致；未动产品实现）                                                                                                                                                                 | `packages/presentation/task/components/task-details/__tests__/`                          |
| T40      | 验收通过（PM 复跑全仓 92 文件 / **805 例全绿**、`guard:ddd` OK；测试文件 mtime 20:59–21:00 早于实现 21:06 → **T39 断言未被触碰**；PRD §5.2 ↔ 冻结串 17 行 PM 逐行比对一致）                                                                      | commit 待提交                                                                            |
| T41      | 验收通过（PM 复核：全仓 93 文件 / **813 例全绿**、`vp check` 全绿、`guard:ddd` OK、双端 build exit 0、移动端零改动；qa 独立复算 L1–L4 缩进 0/4/8/12 与实现公式 `4d` 一致，PM 另读码交叉验证；PRD §5.2 递归规则已补「缩进公式」消除层级命名歧义） | commit 待提交                                                                            |
| T42      | 验收通过（3 提交 pathspec 精确、无 `.agents/**` 夹带、未 push；提交后 §5.2 围栏仍 ```text、`d=0`→0/2/子 4 口径未变）                                                                                                                             | `0527461d` / `facddf27` / `04d90762`                                                     |

## 勘误

- 2026-09-21：本文件曾把 T24/T25 记为「进行中」，与事实不符（无在线会话、无回执、无代码改动）。已作废并更正。原因：编号沿用上一批次且状态先于派发落盘。
- 2026-09-21：`nao-fleet.sh status` 在本环境恒报「未运行」（假阴性）——`running()` 用 `pgrep -f 'pi ... --name <role>'`，但 pi 进程 argv 被清空，`--name` 在进程表不可见。派发前判定改以 `intercom list` 为准；`ensure` 判重同样失效。待修（脚本侧）。
- 2026-09-21：**过程纠偏**——T34 派发范围本不含 T35 的对话框实现，rd-fe 为让 T33 的 6 条对话框红灯转绿而一并实现（**未经 PM 授权越界**）。PM 裁决：不回退（实现正确且测试要求如此），但 T35 取消、验收改由 T37 独立执行。
- 2026-09-21：rd-fe 为通过 `vp check` 按 `.editorconfig`（CRLF）归一了 `.agents/*`、`docs/adr/README.md` 的换行/空白。核查（**当时仅覆盖 git 跟踪文件**）：忽略空白后差异仅剩 6 行，另有 7 个 `.agents` 文件因纯 EOL 差异被还原至 HEAD。未回退。
- 2026-09-21：**上述「仅 6 行」结论口径过窄，已更正**——未跟踪文档（PRD / ADR / 本文件）不在 git 视野内。T37 发现并查实：本次 `vp check --fix` 把 PRD `§5.2 完整拼接示例`（当时全文唯一 ```markdown 围栏）的缩进由 **2/4 重排为 4/8**，**静默改写了冻结格式基准**。全仓复查仅此一处。处置：PM 裁定 **2/4 为准**，恢复原文并把围栏语言改为 `text`（免疫格式化）；改后 `vp check` 全绿且复跑未被改写。
- 2026-09-21：**Fleet 工具链事故（已修，需根治）**——rd-fe 于 20:18 执行 `vp check --fix` 时按 `.editorconfig`（`end_of_line = crlf`、yaml `indent_size = 4`）归一了 `.agents/**`：①`roles.yaml` 缩进 2/4→4/8 且转 CRLF；②5 张角色卡与其余资产的 frontmatter 因 CRLF 不再匹配 `nao-fleet.sh` 的 `^---$` → 「frontmatter role=<缺失>」。**导致 `nao-fleet.sh check` 退出码 1（按卡 §六 本应停止派发）**。处置：`.agents/**` 共 21 个文件还原 **LF**（nao 舰队上游口径）+ `roles.yaml` 缩进还原 2/4 + `codegraph sync`（1 文件）；复验 `check` exit 0。
- 2026-09-21：**更正上一条勘误的判断**——此前用 `git diff --ignore-all-space` 测得「差异仅 6 行」即判为「可忽略噪声」，**该口径错误**：`-w` 会**隐藏整文件 EOL 翻转**，近乎为空的 `-w` 差异恰恰是「全文件 CRLF 化」的特征，而非「只动了 6 行」。教训：涉及 `.agents/**` 的格式化统一，必须显式比对 EOL 与缩进，不得只看 `-w` 差异。
- 2026-09-21：**待决策的根治项**：①`vite.config.ts` 的 `fmt.ignorePatterns` 追加 `'.agents/**'`（现仅 `scripts/**`）——阻断裂队资产被 CRLF 化/重排；②或让 `nao-fleet.sh` 的 awk 解析器 CRLF 容忍（属 nao-skills 资产，宜回流上游）。附带脚本缺陷：`nao-fleet.sh:444` 把 CodeGraph `Pending Changes` 警告记为 `rc=1`，与卡 §六「过期→提醒、不阻塞」冲突，会造成误停。
- 2026-09-21：PM 误报 ADR 路径勘误（误路径实为 T32 intercom 消息摘要中的省略，ADR 正文引用正确），已向架构澄清并撤销。