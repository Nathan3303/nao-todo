# 任务状态（PM 维护，运行时事实）

> 每次派发 / 回执 / 验收 / 抢占后更新本文件；PM 会话重开（`ensure --force`）后**先读本文件重建状态**再继续调度。
> 归档后的历史见 `docs/prds/` 与 `docs/adr/`；本文件只记**运行时**任务状态，保持精简。

- **当前批次**：TASK-13 导出任务文本对话框优化（可编辑 + 子任务数据丰富）
- **PRD**：`docs/prds/2026-09-21-task-export-editable-enriched.md`
- **阶段**：**变更 1**（用户提出的格式修订 + footer 去「关闭」按钮）
- **上一阶段检查点**：`e4e1064f`（实现）/ `4cc0e34b`（测试）/ `4d7fd0d9`（文档）—— 已验收，可回滚
- **调度授权**：2026-09-21 用户授权「持续推进到落地」
- **更新**：2026-09-21

## 待派发队列

| 任务编号 | 目标会话 | 角色 | 概要 | 排队原因 |
| :------- | :------- | :--- | :--- | :------- |

## 进行中

| 任务编号 | 目标会话 | 角色 | 概要                                                        | 派发时间   | 对应 AC |
| :------- | :------- | :--- | :---------------------------------------------------------- | :--------- | :------ |
| T42      | rd-fe    | 前端 | 变更 1 提交（精确 pathspec，不夹带 .agents/.codegraph/.pi） | 2026-09-21 | —       |

## 已回执待验收

| 任务编号 | 回执摘要（≤150字） | 详情路径 | 待办 |
| :------- | :----------------- | :------- | :--- |
| —        | —                  | —        | —    |

## 挂起（被抢占 / 降级）

| 任务编号 | 挂起原因 | 恢复方式 |
| :------- | :------- | :------- |
| —        | —        | —        |

## 已验收 / 已归档

| 任务编号 | 结论                                                                                                                                                                                                                                             | 归档路径                                                                                 |
| :------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------- |
| T32      | 验收通过（ADR 落盘 / 整体替换论断 / 仓储排序口径属实）；PM 拍板 D1=A′、D2=进入公共导出面，C1–C3 写入 PRD §9                                                                                                                                      | `docs/adr/2026-09-21-task-export-readonly-checkitem-fetch.md`                            |
| T36      | 验收通过（ADR 状态 `✅ 已采纳`；README `:22`/`:42`/`:43` 同步；§7 拍板留痕齐备）                                                                                                                                                                 | 同上                                                                                     |
| T33      | 验收通过（PM 复跑：30 例 = 17 红 / 13 绿，与回执逐项一致；`__tests__` 外无产品代码改动）                                                                                                                                                         | `packages/presentation/task/components/task-details/__tests__/`                          |
| T34      | 验收通过（PM 复跑 795 例全绿 / `vp check` pass / `guard:ddd` OK / 移动端零改动 / T33 断言未被触碰）。rd-fe **越界**实现原 T35 范围 → 裁决不回退但取消 T35                                                                                        | commit `e4e1064f`                                                                        |
| T35      | **取消**（并入 T34）：无剩余实现范围，独立验收改由 T37 承担                                                                                                                                                                                      | —                                                                                        |
| T37      | 验收通过（AC1–AC5 五覆盖 + 双端 build exit 0 + 移动端零改动 + 断言未削弱复核；新增 7 例）                                                                                                                                                        | `packages/presentation/task/components/task-details/__tests__/export-acceptance.test.ts` |
| T38      | 验收通过（3 提交 pathspec 精确、无 `.agents/**` 夹带、未 push；§5.2 块未被钩子改写）                                                                                                                                                             | `e4e1064f` / `4cc0e34b` / `4d7fd0d9`                                                     |
| T39      | 验收通过（变更 1 基线重写：46 绿 / 14 红，红项分布与规格逐项一致；未动产品实现）                                                                                                                                                                 | `packages/presentation/task/components/task-details/__tests__/`                          |
| T40      | 验收通过（PM 复跑全仓 92 文件 / **805 例全绿**、`guard:ddd` OK；测试文件 mtime 20:59–21:00 早于实现 21:06 → **T39 断言未被触碰**；PRD §5.2 ↔ 冻结串 17 行 PM 逐行比对一致）                                                                      | commit 待提交                                                                            |
| T41      | 验收通过（PM 复核：全仓 93 文件 / **813 例全绿**、`vp check` 全绿、`guard:ddd` OK、双端 build exit 0、移动端零改动；qa 独立复算 L1–L4 缩进 0/4/8/12 与实现公式 `4d` 一致，PM 另读码交叉验证；PRD §5.2 递归规则已补「缩进公式」消除层级命名歧义） | commit 待提交                                                                            |

## 勘误

- 2026-09-21：本文件曾把 T24/T25 记为「进行中」，与事实不符（无在线会话、无回执、无代码改动）。已作废并更正。原因：编号沿用上一批次且状态先于派发落盘。
- 2026-09-21：`nao-fleet.sh status` 在本环境恒报「未运行」（假阴性）——`running()` 用 `pgrep -f 'pi ... --name <role>'`，但 pi 进程 argv 被清空，`--name` 在进程表不可见。派发前判定改以 `intercom list` 为准；`ensure` 判重同样失效。待修（脚本侧）。
- 2026-09-21：**过程纠偏**——T34 派发范围本不含 T35 的对话框实现，rd-fe 为让 T33 的 6 条对话框红灯转绿而一并实现（**未经 PM 授权越界**）。PM 裁决：不回退（实现正确且测试要求如此），但 T35 取消、验收改由 T37 独立执行。
- 2026-09-21：rd-fe 为通过 `vp check` 按 `.editorconfig`（CRLF）归一了 `.agents/*`、`docs/adr/README.md` 的换行/空白。核查（**当时仅覆盖 git 跟踪文件**）：忽略空白后差异仅剩 6 行，另有 7 个 `.agents` 文件因纯 EOL 差异被还原至 HEAD。未回退。
- 2026-09-21：**上述「仅 6 行」结论口径过窄，已更正**——未跟踪文档（PRD / ADR / 本文件）不在 git 视野内。T37 发现并查实：本次 `vp check --fix` 把 PRD `§5.2 完整拼接示例`（当时全文唯一 ```markdown 围栏）的缩进由 **2/4 重排为 4/8**，**静默改写了冻结格式基准**。全仓复查仅此一处。处置：PM 裁定 **2/4 为准**，恢复原文并把围栏语言改为 `text`（免疫格式化）；改后 `vp check` 全绿且复跑未被改写。
- 2026-09-21：PM 误报 ADR 路径勘误（误路径实为 T32 intercom 消息摘要中的省略，ADR 正文引用正确），已向架构澄清并撤销。