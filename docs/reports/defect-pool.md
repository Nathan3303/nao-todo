# 缺陷池（Defect Pool）

> **性质**：**缺陷的单一权威登记源**（PM 维护）。任何来源发现的缺陷（QA 实机 / 架构评审 / PM 勘察 / 用户反馈）**先登记入池**，再决定是否立单、何时派单。
> **与既有登记的关系**：`docs/adr/README.md` §遗留登记（跨篇）是 **ADR 期**登记（编号 `DEF-<域>-NN`）—— **保留原位不迁移**（历史留痕）；**自 2026-09-23 起，新缺陷一律入本池**。
> **编号规则**：`DEF-N` 产品/纪事级，延续既有 `DEF-1…DEF-4` 序列。`OFF-N` 是 WEB-OFFLINE 勘察期别名（指 `docs/prds/2026-09-23-web-offline-recon.md`）。
> **状态机**：`已登记待派单` → `已派单` → `已回执待验收` → `已闭环` ｜ `已驳回` ｜ `转需求`。
> **严重度**：P1 = 门禁交付 / 数据正确性 / 安全；P2 = 功能语义错误、可绕过；P3 = 观感或口径偏差。

## 一、汇总表

| ID     | 别名  | 标题                                                                                                                                                                                                                                              | 严重度      | 影响面                                                          | 归属                | 前置 / 关联                             | 状态                                                                                                                                                                                                                                                                                                                                                                            |
| :----- | :---- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------- | :-------------------------------------------------------------- | :------------------ | :-------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DEF-5  | OFF-1 | **离线检入会把 JWT 清掉**（网络类失败也 `localStorage.clear()`）                                                                                                                                                                                  | **P1**      | web 离线**完全不可用**                                          | rd-be               | **门禁 WEB-OFFLINE**                    | **已修（T102 / `5b9d6bdb`）**                                                                                                                                                                                                                                                                                                                                                   |
| DEF-6  | OFF-2 | **本地镜像完整性缺口**（≤200 行/表/次启动、最旧优先、无自动补全）                                                                                                                                                                                 | **P1**      | desktop 读不到最新任务                                          | rd-be               | **WEB-OFFLINE 阶段一硬前置**            | **已修（T103 / `ebc2e87b` + T103b / `2a0fdb24`）**（原状态「已实测确证（T2）」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                      |
| DEF-7  | OFF-3 | desktop 未启用 `requestSingleInstanceLock`                                                                                                                                                                                                        | P2          | 多实例并发写                                                    | rd-be               | WEB-OFFLINE 下升 P1                     | **已修（T109 / `2738f320`）**（原「已登记待派单」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-8  | OFF-4 | **全仓零 CSP**                                                                                                                                                                                                                                    | P2          | XSS 面 / 明文姿态下即边界                                       | rd-be + rd-fe       | 明文姿态下**升 P1**                     | **已修（T109 / `2738f320`）**（原「已登记待派单」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-9  | OFF-5 | `getCurrentUserId() ?? ''` 兜底 + `localSession` 内存态                                                                                                                                                                                           | **P1**      | **跨用户读到明文**                                              | rd-be               | 明文姿态前置                            | **已修（T109 / `2738f320`）**（原「已登记待派单」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-10 | OFF-6 | `checkAndCleanExpired` 唯一生产调用点在 `unlock-gate.vue:75`                                                                                                                                                                                      | **P1**      | **注销到期清理永久不触发**                                      | rd-be + rd-fe       | 解锁门退役前置                          | **已修（T105 / `b7612b86`）**（原「已登记待派单」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-11 | OFF-7 | desktop / index list 的 `priority` 排序为**字母序**                                                                                                                                                                                               | P2          | 排序语义错误                                                    | rd-be               | 与 TASK-23 无关（独立发现）             | 已登记待派单                                                                                                                                                                                                                                                                                                                                                                    |
| DEF-12 | OFF-8 | 服务端 `isArchived=false` 空操作 + `sort` 无白名单                                                                                                                                                                                                | P2 / **P1** | 归档漏入 web / **SQL 注入面**                                   | rd-be（server）     | 两子项可分拆                            | **已实测确证（T7）**                                                                                                                                                                                                                                                                                                                                                            |
| DEF-13 | OFF-9 | `syncStatus.pendingCount` 滞后于 `countDirty`                                                                                                                                                                                                     | P3          | 未同步数量显示旧值                                              | rd-fe + rd-be       | 登出清库护栏**不得读它**                | 已登记待派单                                                                                                                                                                                                                                                                                                                                                                    |
| DEF-14 | —     | 服务端 `user_sessions` 行膨胀 + **无过期会话清理**                                                                                                                                                                                                | P2          | 会话表永久累积                                                  | rd-be（server）     | 与 DEF-15 / K10 同源                    | 已登记待派单                                                                                                                                                                                                                                                                                                                                                                    |
| DEF-15 | —     | 客户端 checkin **仅冷启动触发** ⇒ 常驻 >48h 被 10041 强制回登录页                                                                                                                                                                                 | P2          | 桌面常驻用户每 48h 被踢                                         | rd-fe + rd-be       | **与核心诉求直接相关（K8）**            | 已登记待派单                                                                                                                                                                                                                                                                                                                                                                    |
| DEF-16 | —     | **`isUnlocked` 恒假风险**（退役解锁门后无处置位）                                                                                                                                                                                                 | **P1**      | 桌面「离线进入」永久不可达（静默）                              | rd-be + rd-fe       | 门退役前置（K9 / DEF-10）               | **已闭合（T105 `b7612b86`）**                                                                                                                                                                                                                                                                                                                                                   |
| DEF-17 | —     | **web 断网写入失败且无任何反馈**（5 项静默误判）                                                                                                                                                                                                  | **P1**      | **静默数据丢失**（任何网络抖动即触发）                          | rd-fe + rd-be       | 与 WEB-OFFLINE 无关，独立成立           | **已修（T25 / `f650dd39`）**                                                                                                                                                                                                                                                                                                                                                    |
| DEF-18 | —     | **移动端同缺陷副本**：`compose-auth-usecase.ts` 的 `checkIn` 同样「任何错误都 `clearAuthData`」                                                                                                                                                   | P2          | 移动端网络抖动 ⇒ 强制登出                                       | —（**移动端红线**） | 与 **DEF-5** 同源                       | 已登记待授权；**用户裁定 2026-09-23：不修，登记保留**（移动端属项目红线面；如后续移动端出现同类现象，本行即已知根因）                                                                                                                                                                                                                                                           |
| DEF-19 | —     | 看板 `@restore-task` **误调 `delete`**（应为 `restore`）                                                                                                                                                                                          | P2          | 操作方向反（误删面）                                            | rd-fe               | 随 T25 一并修正                         | **已修（T25 / `f650dd39`）**                                                                                                                                                                                                                                                                                                                                                    |
| DEF-20 | —     | `persistence-sync` 日志打印**全量实体 payload** ⇒ **日志 PII 泄漏**                                                                                                                                                                               | **P2**      | 日志泄漏 PII（违反 AC18 禁 PII）                                | rd-be               | 见 §二 明细                             | **已修**（T109b `a23ee1e6`）                                                                                                                                                                                                                                                                                                                                                    |
| DEF-21 | NEW-1 | **web 回退镜像失效**：归一化网络错误（`ERR_NETWORK` / `ECONNABORTED` / `42900`）由 requester **resolve 非 reject** ⇒ `withMirrorFallback` **不回退** ⇒ 离线 web 列表报「网络错误」而非显示镜像                                                    | **P1**      | **AC8 数据面不成立**（阶段一⑤ / C-66 / 目标指标 1「显示数据」） | rd-be               | T111 终局发现（定向探针实测）           | **已修（T112 / `9f0228ba`）**（原「待修（T112）」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-22 | NEW-2 | **明文姿态「用户可见声明」缺失**（ADR §4.5 / U-1 的 RS-3/RS-4 声明：**设置页 + 首次进入告知**）⇒ AC17「设置页明文声明」**无法核验**                                                                                                               | P2          | **D1b「明确告知」未落地** ⇒ 风险接受缺披露                      | rd-fe               | T111 终局发现                           | **已修（T113 / `7b9cb500`）**（原「待修（T113）」未回写，2026-09-23 PM 修正）                                                                                                                                                                                                                                                                                                   |
| DEF-23 | NEW-3 | ADR §5.3 步骤 3 字面「**删除** `unlock-gate.vue`」**未执行**（组件保留）⇒ 口径更正为「**门退役 = 语义退役**」                                                                                                                                     | P3          | ADR 与实际不一致                                                | arch-designer       | T111 终局发现                           | **已闭合（ADR-r6 `aa869db3`）**：措辞按 §5.4 更正为「**进入前提**语义退役；**迁移窗口密码入口按 C-51 保留**」                                                                                                                                                                                                                                                                   |
| DEF-24 | —     | 测试卫生：`offline-entry-positive.test.ts` 残留 `as unknown as` 桥接 cast + **过时注释**（签名已替换）；AC10「零 `markDirty`」为**间接断言**                                                                                                      | P3          | 可读性 / 断言强度                                               | rd-fe               | T111 终局发现                           | 挂账（可选）                                                                                                                                                                                                                                                                                                                                                                    |
| DEF-25 | —     | **回退判据依赖用户可见文案**：仓储边界**只有 `message`**（顶层 `code` 在 Go 仓储返元组时**已丢**）⇒ 网络类判据实为**文案全等匹配**（`50300`/`40800`/`42900` 仅在有 code 时可用）⇒ 文案变更可能使判据失效（`DEF-21` 复发路径）                     | P3          | 潜在静默回退失效                                                | rd-be               | T112 交付时 PM 观察 + **T111b qa 核实** | **挂账（不强制返工**；**可选加固** = 三条归一化文案抽 `packages/shared/requester` 常量供引用；真实 `AxiosRequester` e2e 已能钉死漂移）                                                                                                                                                                                                                                          |
| DEF-26 | —     | `scripts/electron-smoke/**`（5+ 文件）仍按「**必有密码输入**」等待/断言 ⇒ 与「门退役 = 语义退役」不一致（迁移后用户无密码输入 ⇒ **冒烟可能超时/误判**）                                                                                           | P3          | 冒烟测试语义滞后（**当前未被 5 项门禁覆盖**）                   | qa                  | ADR-r6 连带同步项（arch 列出）          | **挂账（下次复跑按新语义调整）**                                                                                                                                                                                                                                                                                                                                                |
| DEF-27 | —     | **窄屏 web（≤445px）抽屉分支不绑定 rail 宿主** ⇒ **无同步状态面板**（AC8/AC9 仍由内容区告警条满足）                                                                                                                                               | P3          | 窄屏 web 缺同步状态入口                                         | rd-fe               | T115 设计（DP-4）                       | **已并入 `DEF-30`（重复项）** —— 两者同为「窄屏 ≤445px 无同步面板」；保留 `DEF-30` 为唯一登记源（2026-09-23 PM 合并）                                                                                                                                                                                                                                                           |
| DEF-28 | —     | **`sync.test.ts` 时间敏感 flaky**：Q3（`errors.length` 期望 1 得 3）**＋ BC-6 / SHELL-06「退避未到期」**（同提交下 isolate 1 红 / no-isolate 全绿 / 两次全仓全绿）                                                                                | P3          | 测试非确定性（可能掩盖真失败）                                  | qa                  | T116 门禁首跑 + **T118 剖析复核**       | **🔴 已升级为必修（`T138`）** —— **反复阻塞批末门禁**（PM 2026-09-23 实测：`sync.test.ts` **BC-3b**「两阶段皆失败 ⇒ lastError 取首个（拉取）」**单跑 ×3 = 1 红 / 绿 / 绿**（~1/3 概率红），症状 = 期望「拉取失败：网络错误」实得「推送失败：网络错误」（`sync.test.ts:880`）；T123 亦复现同一条）⇒ **修法：定位代码侧时序竞争 or 测试侧竞态；断言语义不得放宽，只消除不确定性** |
| DEF-29 | —     | **`nue-ui` 的 `NueAvatar` 内部 `imgError` 不随 `src` 变化复位** ⇒ 一次失败（离线启动/token 过期/CSP 拦截）在本会话内**恒落首字母**                                                                                                                | P3          | 头像一次失败即长期不恢复                                        | rd-fe               | T116 副发现（未改，等 PM 定夺）         | **挂账**（⚠️ `:key="src"` 仅覆盖「src 变化」场景，**不解决瞬态失败后不恢复**）                                                                                                                                                                                                                                                                                                  |
| DEF-30 | N-1   | **窄屏 web（≤445px）抽屉分支无同步状态面板** ⇒ **AC8/AC9/AC13b 在该分支不可验**（**回归**：现状顶部条提供可见性）                                                                                                                                 | **P2**      | 窄屏能力缺口（已显式登记，**非静默降级**）                      | rd-fe + arch        | T115c 设计（P6 / DP-6）                 | **挂账（后续小批次：抽屉亦绑定 `railBottomHost`；需先修订 SHELL-02 C5/AC-08 + 抽屉几何回归）**                                                                                                                                                                                                                                                                                  |
| DEF-31 | —     | **「内容区头部多兄弟块 ⇒ 内容底部被遮住」的布局根因待复现留痕**（用户反馈 #1 的直接诱因；banner 已移出，**但若根因在 `router-view` 容器 flex ⇒ 后续头部内容会复发**）                                                                             | P3          | 布局根因未定位 ⇒ 同类问题可能复发                               | rd-fe（根因证据）   | T115c 连带同步（S13①）                  | **挂账（待 RD 提供复现证据后定级）**                                                                                                                                                                                                                                                                                                                                            |
| DEF-32 | —     | **RD 的「Electron 无渲染帧」原始探针产物未落在 evidence 目录** ⇒ 该「环境阻塞」前提**无法独立核实**（qa 最小独立探针在 **X11 / wayland / hint-auto 三模式均 90–92 帧/1.5s + `ResizeObserver` 回调投递 + visible+focused**）⇒ 实际更像**窗口遮挡** | P3          | 证据留痕不完整 + 结论措辞失真                                   | rd-fe               | T117 qa 独立复核                        | **已闭合**（措辞更正为「窗口遮挡所致」；**真实 Electron 冒烟已由用户 2026-09-23 人机验证全通过** ⇒ 原「待补项」关闭）                                                                                                                                                                                                                                                           |
| DEF-1  | —     | `sync.test.ts > Q3` 偶发失败（测试隔离 / 共享状态泄漏）                                                                                                                                                                                           | P3          | 门禁噪声                                                        | rd-be               | 见 `docs/tasks-state.md` §一            | 已登记待排                                                                                                                                                                                                                                                                                                                                                                      |
| DEF-2  | —     | 舰队资产副本滞后（`nao-fleet.sh` `TASK` 未绑定）                                                                                                                                                                                                  | —           | 已修                                                            | —                   | 见 `docs/tasks-state.md` §一            | **已闭环**                                                                                                                                                                                                                                                                                                                                                                      |
| DEF-3  | —     | `nao-todo-server/.agents` 为 0.2.0 前旧装                                                                                                                                                                                                         | P3          | 跨仓资产                                                        | —                   | 用户决定暂不处理                        | 挂起                                                                                                                                                                                                                                                                                                                                                                            |
| DEF-4  | —     | 日历日视图测试**日期时间炸弹**                                                                                                                                                                                                                    | —           | 已修 `5d2e51f1`                                                 | —                   | 见 `docs/tasks-state.md` §一            | **已闭环**                                                                                                                                                                                                                                                                                                                                                                      |

## 二、逐条明细

### DEF-5（OFF-1）离线检入清 JWT —— **P1**

| 项           | 内容                                                                                                                                                        |
| :----------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `AuthUseCase.checkIn` 在**任何错误（含网络类）**时执行 `authStore.clearAuthData()` → `clearUserData()` → **`localStorage.clear()`**                         |
| **症状链**   | web 离线刷新 → 门 B（`routes.ts` `beforeEnter`）拦下 → 走 checkin → 网络失败 → **JWT 被销毁** → 重试读到空 token → 落登录页（离线无法登录）= **彻底进不去** |
| **影响**     | web 离线**完全不可用**；且说明 SHELL-05 C-24「网络类不跳 signin」的意图在 usecase 层被 `clearAuthData()` 反噬                                               |
| **证据**     | arch A3 §0.2（读码确认；Z2 待实测补充）。原报告 `/tmp/WEB-OFFLINE-A3-offline-boundaries.md`（已沉淀至 recon 文档）                                          |
| **建议处置** | 区分**网络类**与**凭证类**失败：网络类**不得** `clearAuthData`；凭证类（401/403/10041）保持现状                                                             |
| **验收要点** | ① 断网冷启动/刷新后 **JWT 仍在**且可走离线进入；② 凭证类失败仍清认证；③ 不能把「网络抖动」变成「强制重新登录」                                              |

### DEF-6（OFF-2 / C5）本地镜像完整性缺口 —— **P1**

| 项           | 内容                                                                                                                                                                                                                                                                                                                                                           |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `PULL_LIMIT = 200`/表、排序 `updated_at ASC`（**最旧先拉**）、`ListSync` 用 `.Unscoped()`（墓碑也占窗口）、`pullAllInner()` **单轮无续拉**；拉取触发点**仅 3 处**（`start()` / `pullAll()` / `manualSync()`）；而 `online` + `visibilitychange` 走的 `resumeBackfill()` **只 push 不 pull**，且 `scheduleBackfillTick` 在 `countDirty === 0` 时**直接 return** |
| **影响**     | 大账号下本地副本以「**最旧优先、≤200 行/表/每次启动**」填充且**无自动补全** ⇒ **desktop 可能读不到最新任务**。dev 库全表 **189 行 < 200**，**恰好掩盖了它**                                                                                                                                                                                                    |
| **证据**     | arch A1/A2/A3（读码 + 推断）；**运行时确证待 V1 探针**                                                                                                                                                                                                                                                                                                         |
| **建议处置** | 连续拉取（循环至无更多数据）；`resumeBackfill` 补 pull；触发点补全                                                                                                                                                                                                                                                                                             |
| **验收要点** | ① >200 行账号：启动 1 次后 `tasks.count()` **不应**停在 200（V1）；② 本地必须包含**最新**任务 id                                                                                                                                                                                                                                                               |
| **关联**     | **WEB-OFFLINE 阶段一硬前置**；**V1 探针需 >200 行账号**（现状 189 行会给出**假阴性**，必须先造数）                                                                                                                                                                                                                                                             |

### DEF-7（OFF-3）desktop 未启用单实例锁 —— P2（明文姿态下 P1）

| 项           | 内容                                                                                                                                                                                                                                           |
| :----------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `requestSingleInstanceLock` **实测未启用**（A2 G9）                                                                                                                                                                                            |
| **影响**     | 桌面可多开 ⇒ 多写者：① pull 游标 RMW ② 重复 push（`schedulePush` 每标签各排）③ 暂停/退避态互相覆盖 ④ **迁移并发**（先完成者删 `key-bundle`、后到者失败）⑤ **「登出复活」**（标签 A 清库、标签 B 内存 store 写回 ⇒ 明文复活）⑥ 双实例双倍首同步 |
| **建议处置** | desktop 加单实例锁（**一行配置、收益最大**）；web 用 `BroadcastChannel` 广播 logout 让其他标签清 store 并跳登录（同时解 ⑤）；迁移/同步用 Web Locks 选主                                                                                        |
| **验收要点** | 双开被拒绝；多标签登出后任一标签均无残留明文                                                                                                                                                                                                   |

### DEF-8（OFF-4）全仓零 CSP —— P2（明文姿态下 P1）

| 项           | 内容                                                                                                                                                                                                                                                                                                                                                         |
| :----------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `securityHeaders()` 仅 `X-Frame-Options` / `X-CTO` / `X-XSS-Protection`(已废弃) / `Referrer-Policy`；**无 CSP**                                                                                                                                                                                                                                              |
| **约束**     | ① desktop 生产走 **`file://`**（`win.loadFile`）⇒ 无响应头 ⇒ **CSP 只能靠 `<meta>`**；且 Chromium 下 `file://` 的 localStorage/IndexedDB 归属「file 协议」大类 ⇒ 同 profile 下其它本地 HTML 也可能触及同源存储；② **SPA 不由 `nao-todo-server` 托管** ⇒ CSP 需落**静态托管侧**；③ API 是**另一个 origin**（`VITE_BASE_URL`）+ `withCredentials`（带 cookie） |
| **影响**     | 明文姿态下 **origin 独占性即机密性边界** ⇒ CSP 从「建议」升为「必须」                                                                                                                                                                                                                                                                                        |
| **建议处置** | 托管侧响应头 CSP + desktop `<meta>` CSP；移除已废弃的 `X-XSS-Protection`；第三方资源按域白名单                                                                                                                                                                                                                                                               |
| **验收要点** | 两端均存在生效 CSP；无 `unsafe-eval`；加载/交互无 CSP 违规报错                                                                                                                                                                                                                                                                                               |

### DEF-9（OFF-5）`getCurrentUserId() ?? ''` 兜底 + `localSession` 内存态 —— P1（明文姿态下）

| 项           | 内容                                                                                                                |
| :----------- | :------------------------------------------------------------------------------------------------------------------ |
| **机制**     | `getCurrentUserId()` 以 `?? ''` 兜底；`localSession.currentUserId` 是**内存态**（刷新即 null），且 **web 无重建点** |
| **影响**     | 明文落盘下**极易跨用户读到明文**；`?? ''` 使「无用户」与「用户空串」不可区分                                        |
| **建议处置** | `userId` 隔离改为**硬失败**（null/空一律拒绝库操作）；web 启动时从 JWT 重建 session                                 |
| **验收要点** | 无/空 userId 时库操作被拒绝并可见；跨账号切换不串数据                                                               |

### DEF-10（OFF-6）`checkAndCleanExpired` 唯一生产调用点在被退役的门上 —— P1（门退役即触发）

| 项           | 内容                                                                                                                                                                                                                                                                                                                                 |
| :----------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | 已存在「按用户事务清空全部本地数据」原语 `deletionService.checkAndCleanExpired()`（事务清 11 业务表 + `meta` + `syncQueue` + `syncCursor`（**⚠️ `deletionSchedules` 不在 wipe 范围内** —— T106 后口径：仅 `checkAndCleanExpired` 在 wipe 后删**自己的** schedule；C-52 r5），已单测）；但其**唯一生产调用点在 `unlock-gate.vue:75`** |
| **影响**     | 退役解锁门时若不迁走该调用 ⇒ **注销到期清理永久不触发**（**静默失效、无报错**）                                                                                                                                                                                                                                                      |
| **建议处置** | 门退役时同步把调用迁到新启动路径（并入 WEB-OFFLINE 门退役顺序步骤 5）                                                                                                                                                                                                                                                                |
| **验收要点** | 门退役后，注销宽限期到期仍触发本地清理（有可观测断言）                                                                                                                                                                                                                                                                               |

### DEF-11（OFF-7）`priority` 排序为字母序 —— P2

| 项           | 内容                                                                                                                                   |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | local 仓通用排序对字符串用 `String(a).localeCompare(String(b))` ⇒ `priority` 取值 `low`/`medium`/`high` 时 `asc` = `high, low, medium` |
| **影响**     | desktop / index list 的「按优先级排序」**语义错误**（两个方向都不是优先级序）                                                          |
| **证据**     | arch A2 §B（读码 + node 实测）                                                                                                         |
| **建议处置** | 比较器加 **rank 映射**（`low 0 / medium 1 / high 2`，服务端另存在 `urgent 4` ⇒ 应含 `urgent 3`）；**或**前端 rank 排序                 |
| **验收要点** | `asc`/`desc` 两端均为语义序；含 `urgent` 时的兜底与用例                                                                                |

### DEF-12（OFF-8）服务端 `isArchived` 空操作 + `sort` 无白名单 —— P2 / **P1（安全）**

| 子项                              | 内容                                                                                                                                                                           |
| :-------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **① `isArchived=false` 是空操作** | 归档任务**漏入 web 结果**（数据可见性错误）。建议：修过滤语义。验收：归档任务不出现在默认列表                                                                                  |
| **② `sort` 无白名单**             | `ORDER BY ToSnakeCase(field) direction` 原样拼接、**无白名单** ⇒ **SQL 注入面**（arch 建议另开安全 ticket）。建议：字段与方向**双向白名单**。验收：非法 field/direction 被拒绝 |
| **证据**                          | arch A1 §A / A2 §C（读码 + 实测）                                                                                                                                              |

### DEF-13（OFF-9）`pendingCount` 滞后 —— P3

| 项           | 内容                                                                                                        |
| :----------- | :---------------------------------------------------------------------------------------------------------- |
| **机制**     | `syncStatus.pendingCount` **只在一次同步运行的 `endRun()` 时刷新** ⇒ 写完 2s 内、或从未跑过同步时是**旧值** |
| **影响**     | 任何「未同步数量」UI 可能显示旧值；**登出清库护栏若读它会误判**（漏判/误判未同步数据）                      |
| **建议处置** | 权威口径统一用 `syncTracker.countDirty(userId)`；UI 明确刷新时机                                            |
| **验收要点** | 写入后立即读到的数量与 `countDirty` 一致                                                                    |

### DEF-14 服务端 `user_sessions` 行膨胀 + 无过期会话清理 —— P2

| 项                          | 内容                                                                                                                                                                                                                       |
| :-------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**                    | 建会话以 `(user_id, device_id)` 唯一索引 upsert；但 **`device_id` 为空时不参与去重**（每次登录新增行）；服务端 cron **仅 2 个任务**（`DeleteDeactivedUserJob(7)` + 任务提醒扫描）⇒ **无过期会话清理** ⇒ 失效行**永久累积** |
| **实测（dev 库只读，Z11）** | `user_sessions` = **61 行 / 11 用户**；**12 行 `device_id` 为 NULL 或空**；单用户最多 **19 行（其中 17 行已过期）** ⇒ **膨胀已真实存在**                                                                                   |
| **关联**                    | **DEF-15**（客户端 deviceId 被 `localStorage.clear()` 误清 ⇒ 空 deviceId ⇒ 加剧膨胀）、**K10**（E6 白名单 `nao.deviceId` 必留）                                                                                            |
| **建议处置**                | 服务端补过期会话清理任务；客户端保住 `deviceId`（K10）；`device_id` 空值处理策略                                                                                                                                           |
| **验收要点**                | 过期会话可被回收；同一设备重复登录**不新增行**                                                                                                                                                                             |

### DEF-15 客户端 checkin 仅冷启动触发 ⇒ 常驻 >48h 被强制回登录页 —— P2

| 项           | 内容                                                                                                               |
| :----------- | :----------------------------------------------------------------------------------------------------------------- |
| **机制**     | `check-in.vue:37` 是 checkin 的**唯一**触发点 = **仅冷启动**，无周期性；JWT **48h**、会话 **7 天绝对期**（不滚动） |
| **影响**     | **桌面常驻用户每 48h 被强制回登录页**（与是否活跃**无关**）；7 天后连 checkin 都失败 ⇒ 只能 `signIn`（**需密码**） |
| **关联**     | **K8**：若用户要求「永不弹密码框」，此项与 7 天绝对期即为其**硬上限**                                              |
| **建议处置** | 补周期性/前台恢复时的 checkin（滚动续期）；或明确接受"每 7 天需密码一次"                                           |
| **验收要点** | 常驻 7 天以上不被强制登出（或明确记录为已接受行为）                                                                |

### DEF-16 `isUnlocked` 恒假风险（退役解锁门后） —— P1

| 项           | 内容                                                                                                                                                                                                                     |
| :----------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `isUnlocked` getter = `dek !== null`，而 `dek` **只由 `setup()` / `unlock()` 置位**（`crypto-service.ts:56,150-175`）⇒ **退役 `UnlockGate` 后若无处再调 `unlock()`，`isUnlocked` 恒假**                                  |
| **影响**     | 桌面端「离线进入」**立即永久不可达**；且属 **silent dead code**（不报错，**测试也发现不了** —— 现有测试全部 mock 成 `true`）                                                                                             |
| **建议处置** | 见 **K9**：**(a)** passthrough 仍须置位 `isUnlocked`（ADR 明确）；**(b)（建议）** 同批删条件④、改用「本地镜像存在且 `userId` 匹配」，**两处实现同步改 + 补正向单测**（现有测试只覆盖 `isUnlocked=false` 的**拒绝**路径） |
| **验收要点** | 退役后离线进入仍可达（**正向**断言，非仅拒绝路径）                                                                                                                                                                       |

### DEF-10 补充：新调用点设计（Z10 探针结论，2026-09-23）

| 项             | 内容                                                                                                                                                                                                                                                                                                                                                                                                                       |
| :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **硬约束**     | ① 需要 `userId`（来自 `resolveUserIdFromStoredJwt`）② **必须早于 `syncService.start()`**（`start()` 读 `deletionSchedules` 决定是否跳过启动同步）③ 必须在**所有「进入 App」路径**上执行                                                                                                                                                                                                                                    |
| **建议**       | 抽**单一收敛点** `bootstrapLocalData(userId?)`：`userId ??= resolveUserIdFromStoredJwt()` → `localSession.setCurrentUserId(userId)`（**顺带修 DEF-9 的 `?? ''` 兜底**）→ `await deletionService.checkAndCleanExpired(userId)`                                                                                                                                                                                              |
| **调用点穷举** | ① **desktop 冷启动**（`AppRoot` 在渲染任一门前 await，替代 `unlock-gate.vue:75`，保证早于 `InitialSyncGate.start()`）——**必须**；② **web 阶段一**门 B 通过后挂载 App 前的等价收敛点（web 无 `AppRoot`）——**必须**；③ **常驻跨过 7 天** ⇒ 挂到已存在的 `registerBackfillTriggers` 前台恢复/`online` 触发源再跑一次——**建议**（否则"从不重启的桌面应用"永久不清理）；④ 注销当刻已由 `recordDeletion` 覆盖；⑤ 暖启动由 ③ 兜底 |
| **顺序硬约束** | 迁走**必须早于**删除 `unlock-gate.vue`；同一 PR 须「**先加新调用点 + 单测 → 再删旧门**」。⚠️ 现有覆盖单测在 `unlock-gate.test.ts`（随门消失）⇒ **新调用点必须自带单测**                                                                                                                                                                                                                                                    |
| **可验收判据** | 一条**顺序断言**：`InitialSyncGate.start()` 被调用前 `checkAndCleanExpired` 已被调用（探针 Z14 预期**无**此断言，需补）                                                                                                                                                                                                                                                                                                    |

### DEF-17 web 断网写入失败且**无任何反馈**（5 项静默误判） —— P1

| 项             | 内容                                                                                                                                                                                                                                                                                                                                    |
| :------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**       | web 侧写路径**全部服务端直连**（`*RepoImpl(requester)`）；断网请求被 `requester` **归一化为 `Promise.resolve(...)`（不 reject）** ⇒ 仓储只判业务码 ⇒ **仅 toast 或静默**                                                                                                                                                                |
| **实测（T3）** | 断网勾选 ⇒ **`PUT /api/tasks/:id` ×3 被阻断** ⇒ toast「网络错误…」；而下列 **5 项连 toast 都没有**：**#9** 看板拖拽改状态、**#10** 看板完成/取消（**返回值被丢弃**）、**#11** 创建子任务、**#13** 子任务拖拽改序（待实测）、**#19** **番茄结束落库（仅 `console.error`）**；另 **#23** 外观偏好（有 toast 但主题已本地应用 ⇒ 部分误判） |
| **影响**       | **任何网络抖动即静默丢写**（不限于离线场景）⇒ 用户以为已保存。**与 WEB-OFFLINE 是否立项无关，独立成立**                                                                                                                                                                                                                                 |
| **对照**       | **desktop 不受影响**：全部走 `newLocal*Repository()`（Dexie + `markDirty` **入队**）⇒ 断网星标/勾选**无 `PUT`**、仅 `/sync/push` 被阻断、本地时间已更新 ⇒ **不丢、不误判**                                                                                                                                                              |
| **建议处置**   | ① 先修 5 项静默误判（至少给可见失败反馈）；② 阶段一「只读」需**在 UI 层统一禁写全部 24 项**（清单见 `docs/reports/2026-09-23-DEF-PROBE-P1-offline-probes.md` §3.3），**不要逐个改仓储**                                                                                                                                                 |
| **验收要点**   | 断网下任一写入口均有**可见失败反馈**；无「看似成功实则丢失」路径                                                                                                                                                                                                                                                                        |

### DEF-8 补充：实测证据（T4 / T5，2026-09-23）

| 项                 | 实测结论                                                                                                                                                                                                                                         |
| :----------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **desktop 驱逐**   | **`navigator.storage.persisted() === true`**（默认持久，**不受浏览器驱逐**）⇒ 修正 A1/A2 的推断口径                                                                                                                                              |
| **web 驱逐**       | Chromium best-effort（压力下按 origin LRU 驱逐，`persist()` 仅启发式授予）；**Safari/WebKit ITP：7 天无交互即删全部脚本可写存储**（WebKit 官方口径）。**全仓零 `persist()` 调用 ⇒ web 驱逐风险 100% 暴露**                                       |
| **`file://` 归属** | **确证跨目录共享**（Electron 43 实测）：pageA 写入 → 同目录 pageB **与不同目录 pageC** 均读到 localStorage 与 IndexedDB；`location.origin === "file://"` ⇒ **同 profile 下任何本地 HTML 都能读本应用落盘数据**（明文姿态下即**机密性边界失效**） |

### DEF-12 补充：实测证据（T7，2026-09-23）

| 子项               | 实测结论                                                                                                                      |
| :----------------- | :---------------------------------------------------------------------------------------------------------------------------- |
| `name` 语义        | **子串 + 大小写不敏感**（`alpha`=3、`ALPHA`=3；根因 `LIKE '%…%'` + collation `utf8mb4_0900_ai_ci`）                           |
| `%` / `_`          | **未转义**（`name=%` → 7/7、`_` → 7/7）⇒ **LIKE 通配符枚举面**                                                                |
| `isArchived=false` | **确证漏入归档**（7 行含 `ARCHIVED-LEAK`）；`isArchived=true` → 1 行 ⇒ `ByTaskArchived(false)` 是 no-op                       |
| `sort=priority`    | **数值 rank 正确**（none → low×3 → medium → high → urgent）⇒ 服务端排序无 DEF-11 问题（DEF-11 是 **desktop 本地仓**的比较器） |
| `sort` 白名单      | **确证无白名单**：`sort=id;SELECT 1:asc` → HTTP 200 / code 40052，且**回显 MySQL 语法错误** ⇒ **注入面确证**                  |
| `pagination`       | 返回 `{total,page,limit,maxPage}` ✓                                                                                           |

### DEF-18 移动端同缺陷副本（`presentation-react`） —— P2

| 项           | 内容                                                                                                                                                                 |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `packages/presentation-react/src/logic/compose-auth-usecase.ts` 的 `checkIn` **同样在任何错误（含网络类）时 `store.clearAuthData()`**（实测 `:69` / `:80` 两处调用） |
| **影响**     | 移动端网络抖动/离线 ⇒ **强制登出**（与 DEF-5 在 web 侧的后果同源）                                                                                                   |
| **发现来源** | `rd-be-T102` 修复 DEF-5 时**发现并上报、未擅自改动**（正确处置 —— 移动端为**项目红线**：`packages/presentation-react` / `apps/mobileapp` 不经用户授权不得改动）      |
| **建议处置** | **需用户授权**后方可修；修法与 DEF-5 一致（复用 `domain-identity` 导出的 `isCredentialError` 做**同一套**失败分流，避免第三份标记集）                                |
| **验收要点** | 移动端网络类失败保留 JWT；凭证类仍清                                                                                                                                 |

### DEF-17 补充：探针口径修正（T25 实测，2026-09-23）

| #   | 修正                                               | 说明                                                                                                                          |
| :-- | :------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------- |
| 1   | **#8「表格/列表行删除·恢复」可见性：toast → 静默** | 报告 §3.3 原标 toast，T25 读码实测为**静默**（adapter 内联 `taskUseCase.delete/restore` **丢弃返回值**）⇒ 已由 T25 补可见反馈 |
| 2   | **清单漏项：看板·删除/恢复**                       | 24 项清单**漏**该入口（`kanban-view-adapter` 内联丢弃返回值）⇒ 补入后**清单为 25 项**；已由 T25 补                            |
| 3   | **所属清单口径**                                   | **AC10 / ADR C-59 不再引用硬计数**，改为**以报告 §3.3 为唯一真源**（避免后续增补再次造成计数漂移）                            |

### DEF-19 看板 `@restore-task` 误调 `delete` —— P2

| 项           | 内容                                                                                                                                      |
| :----------- | :---------------------------------------------------------------------------------------------------------------------------------------- |
| **机制**     | `kanban-view-adapter.vue` 的 `@restore-task` 事件**误调 `delete`**（应为 `restore`）                                                      |
| **影响**     | 操作方向相反：若触发点指向**未删除**任务 ⇒ **误删**；指向**已删除**任务 ⇒ 多为**幂等 no-op** ⇒ 影响有限，但**语义错误明确**               |
| **来源**     | `rd-fe-T25` 施工中发现的**相邻 1 行** bug ⇒ **已随 T25 一并修正**（PM 判为在范围内：属「#10 看板删除/恢复」这一入口本身，非无关相邻代码） |
| **验收要点** | 看板「恢复」调用 `restore`（有断言/回归例覆盖）                                                                                           |

### DEF-20 `persistence-sync` 日志打印全量实体 payload ⇒ 日志 PII 泄漏 —— P2（**已修**）

| 项           | 内容                                                                                                                                                                                                                         |
| :----------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **现象**     | 推送路径用 `console.log` 打印**完整实体 payload**：`console.log('[sync] 推送请求 /sync/push 样本', JSON.stringify(...))`、`console.log('[sync] 推送响应', JSON.stringify(response?.data))` ⇒ **任务正文 / 用户数据进入日志** |
| **影响**     | 违反 **AC18「禁 PII」**；日志若被采集/上报（含服务端侧）即构成 **PII 泄漏面**                                                                                                                                                |
| **发现**     | `rd-be-T109b` 施工 AC18 结构化日志时**读码发现**（**PM 判为在范围内**：AC18 的「禁 PII」本就要求覆盖**既有**日志面，非新增功能）                                                                                             |
| **修复**     | 改为**计数 / 枚举字段**，并统一走 `packages/infrastructure/src/observability/structured-log.ts`（该层**单一真源**，含 PII 脱敏 + 有界缓冲）                                                                                  |
| **验收要点** | 断言 `JSON.stringify(readStructuredLog())` **不含** 任务正文 / email / token（T109b 3 处测试覆盖：`structured-log` / `ac18-key-paths` / `pull-single-master`⑦）                                                              |

## 三、前置关系（与 WEB-OFFLINE 立项）

| WEB-OFFLINE 步骤             | 被门禁的缺陷         | 说明                                                                 |
| :--------------------------- | :------------------- | :------------------------------------------------------------------- |
| **阶段一「离线能看」**       | **DEF-5**、**DEF-6** | 不修 DEF-5 ⇒ web 离线进不去；不修 DEF-6 ⇒ 镜像不完整，「能看」不成立 |
| **安全姿态改造（两端明文）** | **DEF-8**、**DEF-9** | 明文下 CSP 与 userId 隔离从「建议」变「准入条件」                    |
| **解锁门退役**               | **DEF-10**           | 漏迁调用 ⇒ 注销清理永久不触发                                        |
| **多标签（阶段二）**         | **DEF-7**            | 单写者假设被破坏，「登出复活」为新增暴露                             |

> **登出清库护栏注意**：判定「是否有未回传写入」**必须直接调 `countDirty`**，**不得读 `syncStatus.pendingCount`**（DEF-13）。

| 2026-09-23 | **P1 实机探针回执入档**：DEF-5（T6）、DEF-6（T2）、DEF-12（T7）→ **已实测确证**；**新增 DEF-17**（web 断网写无反馈，5 项静默误判，P1）；DEF-8/DEF-12 补实测证据 |

| 2026-09-23 | **T102 验收通过**（DEF-5，提交 `5b9d6bdb`）；**新增 DEF-18**（移动端同缺陷副本，属项目红线 ⇒ 待用户授权）；DEF-5 状态 → **已修（待批内红窗口闭合）** |

| 2026-09-23 | **T25 验收通过**（DEF-17，`f650dd39`，11 文件）；**新增 DEF-19**（看板 `@restore-task` 误调 `delete`，随 T25 修正）；DEF-17 补 3 项探针口径修正（#8 静默 / 漏项 ⇒ **清单 25 项** / 改以 §3.3 为唯一真源）；`vp check` 已回 **exit 0**（PM 独立验证） |

## 四、历史条目（不迁移，仅索引）

| 位置                                   | 编号                                                      | 说明                                          |
| :------------------------------------- | :-------------------------------------------------------- | :-------------------------------------------- |
| `docs/adr/README.md` §遗留登记（跨篇） | `DEF-STORE-01/06/07`、`DEF-SYNC-01/04/05/06`、`DEF-UI-01` | ADR 期登记，**保留原位**；各自有对应 ADR 文档 |
| `docs/tasks-state.md` §一 需求池       | `DEF-1`…`DEF-4`                                           | 本池汇总表已含索引，**详细过程留痕**见该文件  |

## 五、变更记录

| 日期       | 变更                                                                                                                                                                                                                                                    |
| :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-23 | 首次成文：建立**单一缺陷池**；登记 `DEF-5`…`DEF-13`（来源 arch A1/A2/A3 核验与 TASK-23 讨论中的独立发现，别名 OFF-1…OFF-9）；收录 `DEF-1`…`DEF-4` 索引；登记与 WEB-OFFLINE 的前置关系                                                                   |
| 2026-09-23 | **探针批次已派**：`P1`（`qa`：T1 造数 >200 行 + **T2 确证 DEF-6** + T3 离线误判入口清单 + T4/T5/T6/T7）；`P2`（`arch-designer`：Z1/Z5/Z7/Z10 + 14 项删除项影响评估）。DEF-6 状态 → `已派探针确证中`。**凭据走 `NAO_QA_*` 环境变量、不经 intercom 传递** |
| 2026-09-23 | 登记 `DEF-20`（`persistence-sync` 日志打印全量实体 payload ⇒ **PII 泄漏**；**已修** T109b `a23ee1e6`）—— 由 T109b 施工 **AC18** 时**读码发现**（PM 判为在范围内）                                                                                       |