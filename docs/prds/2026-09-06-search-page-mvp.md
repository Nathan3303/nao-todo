# 2026-09-06 搜索页面 MVP（SEA-01）验收归档

- **交付**：/search 落地（关键词回找全部任务）+ DEF-1（详情加载失败→后端限流拆桶）+ FE 退避配合
- **commit**：`4d0bf261`（SEA-01）、`36dcffd0`（FE DEF-1 限流退避/错误分级）、后端 `88700cd`（REL-1 tasks-read 拆桶，nao-todo-server）
- **终签**：✅ 通过（用户 E2E/联调冒烟 2026-09-06）

## 1. 主题与背景（5 Whys 摘要）

导航占位长期"敬请期待" → 任务多后"回找旧任务"靠翻找 → **以关键词直达任何历史任务的回找通道**（查全优先、结果可达）。假设：跨清单找旧任务为高频路径（置信中，待埋点验证），个人数据量级下全量本地过滤可支撑。

## 2. 目标指标

`search_query`（词/结果数/数据规模/过滤耗时）、`search.result_click`（rank/taskId）、`search.cap_hit` —— 埋点 P1 统一接入；无埋点期间冒烟验收。

## 3. 决策留痕（用户逐项确认）

| 决策             | 结论                                                                                                               |
| :--------------- | :----------------------------------------------------------------------------------------------------------------- |
| 对象/字段        | 任务实体 名称+备注 子串（OR）；覆盖全部清单/默认容器/已完成；排除 deleted/archived/given-up                        |
| 结果组织         | 单一扁平列表；点行开内嵌详情（复用 TaskDetails 适配）；无结构化筛选（后置→SEA-03）                                 |
| 数据口径         | 客户端全量本地过滤（懒加载全量 ≤5000、会话缓存、激活刷新、未就绪重放、去抖 200ms）                                 |
| 子任务（方案 A） | 顶层全量 + 键入时按父任务 4 并发补拉直接子层归并缓存（单层假定；深层仅补一层不递归）；顶层结果先行、子任务并入提示 |

## 4. 关键事件与修复

- **SEA-01 DEF-1（详情 drawer「加载失败」）**：根因=后端 Redis 固定窗口 60s/IP `/tasks` 全家共享 48/min 桶，搜索枚举突发打爆后详情同桶被拒（HTTP 200+code 10051）。解决（用户批方案 A）：后端 REL-1 将 GET list 拆 `tasks-read`（400/min/IP），详情/写维持 tasks 48（`88700cd`）；前端退避重试+枚举暂停恢复+错误分级（`36dcffd0`）。实测：250 连发 list 无 10051；详情/写仍受 48 约束；5000 条本地过滤 10.82ms。

## 5. 验收

SEA-1~~10 + FE-DEF1-1~~3 + BE-REL1-1~4 全过（单测 +14 首轮、退避 +4）；门禁 lint/vue-tsc(0 新增)/vitest/builds 全绿；联调冒烟通过（详情四类行、枚举两档、日历/任务页无回归）。

## 6. 遗留项与待重新评估

- **P1**：search 埋点事件接入。
- **P2**：服务端全文/批量 parentTaskIds（④a）按需立项；按 userId 计数与 429+Retry-After（④b/④c）上线前评估；极窄格/超大数据策略。
- 后续 SEA-02（节点样式）、SEA-03（筛选栏）另档归档。