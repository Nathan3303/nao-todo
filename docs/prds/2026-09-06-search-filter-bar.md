# 2026-09-06 搜索页筛选栏（SEA-03）验收归档

- **交付**：搜索框下四维筛选栏（清单/标签/优先级/状态，多选）+ 空/错状态对齐任务子视图 + DEF-1（触发器 NueButton+#append）+ DEF-2（loading 滞留修复）
- **commit**：`48079667`（SEA-03）、`b874ec4e`（DEF-1）、`77cde7a9`（DEF-2）；用户样式先行 `cca0c161`
- **终签**：✅ 通过（用户 E2E/冒烟 2026-09-06）

## 1. 主题与背景（5 Whys 摘要）

关键词回找靠记忆拼词 → 用户已知属性（清单/优先级/完成态）却无法组合收敛 → **结构属性缩小回找空间，关键词×属性组合过滤**。假设：清单+关键词为高频定位路径（埋点 `search.filter_apply` P1 验证）。

## 2. 决策留痕（用户逐项确认）

| 决策                  | 结论                                                                                                             |
| :-------------------- | :--------------------------------------------------------------------------------------------------------------- |
| 栏形态                | 搜索框下一栏四枚内联下拉（多选+计数+激活高亮+一键清空）                                                          |
| 取值                  | 清单=收件箱(projectId='' 哨兵)+用户清单；标签全量带色点；优先级高/中/低；状态 未开始/进行中/已完成（弃置不提供） |
| 关系                  | 关键词 AND（维内 OR）维间 AND；空选=不限（默认含已完成不变）；属性即时/关键词去抖                                |
| 空/错态               | 对齐任务子视图 Adapter：LoadingError + error.webp/notaskhere.webp + #error/#empty；无结果双清入口                |
| 触发器（DEF-1）       | 维度按钮改 NueButton，已选情况放 #append（N 徽标）；未选 ghost/已选激活底+徽标                                   |
| loading 修复（DEF-2） | 内层 LoadingError 显式 `:loading="false"`（组件默认 true 导致"永加载"）                                          |

## 3. 关键事件

- **DEF-2（搜索后永远加载中）**：根因=entry scroll 区内层 `<loading-error v-else>` 未传 `:loading`（默认 true 且 v-if 第一优先）→ 关键词非空即渲染加载占位遮蔽结果。状态机本身无滞留；修复=1 行。协调方式 A：用户先提交样式（`cca0c161`），逻辑独立提交（`77cde7a9`），diff 复核零样式触碰。

## 4. 验收

SEA-3-1~~11 + DEF1-1~~3 + DEF2-1~4 全过；纯层单测 +7（哨兵/tag OR/组合/空数组/排序/性能）；5000 行四维组合过滤 8.42ms；空/错态与任务子视图观感一致；无字面量色（rg 0）；明暗双主题通过；日历/任务页零回归。

## 5. 遗留项与待重新评估

- **P1**：`search.filter_apply` 埋点。
- **P2**：筛选持久化（URL/本地）、结果按属性分组、服务端筛选；④b/④c 后端候选（按 userId 计数、429+Retry-After）。