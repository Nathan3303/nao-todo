# 2026-09-06 日历周起始偏好（CAL-06）验收归档

- **交付**：feat(calendar) — C9 周起始偏好（周日/周一）+ DEF-1（周视图日期号对齐月视图）
- **commit**：`02a3ec3d`（CAL-06）、`318a8488`（DEF-1）
- **终签**：✅ 通过（用户冒烟 + 自行样式微调不影响验收，2026-09-06）

## 1. 主题与背景（5 Whys 摘要）

日历固定周日开头与部分个人习惯不符 → 无周一起始偏好 → 用户需脑内换算 → **布局口径应服务个人时间习惯**。节假日因无可靠数据源已裁剪（决策留痕）。

## 2. 目标指标

无独立指标；偏好使用率冒烟（埋点 `calendar.weekstart_change`，P1）。

## 3. 9 模块要点

| 模块     | 定稿                                                                                                                                                                                                                   |
| :------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 决策记录 | 侧栏「日历概览」区「周起始：周日/周一」切换；localStorage **`CALENDAR_WEEKSTART`**（sunday/monday，默认 sunday，非法回退规范化）；影响=月历列起始+星期表头+周末标记+周视图定周/导航/标题；任务页"本周"数据语义不在范围 |
| 规则     | 起始偏移 `(first.day()−offset+7)%7`（sunday=0/monday=1）；切换即时重排零重拉；今天/选中/筛选/未安排语义不变；多日条/动态轨道/条带以列为相对基准                                                                        |
| 架构     | 口径下沉 monthly-layout 纯函数（weekStart 参数化，默认 sunday 零行为变化）；偏好=UI 状态（Context 仅传 UI ref）——遵循 frontend-developer.md 角色纪律                                                                   |
| AC       | C9-1~6 + 边界（非法值回退）+ 负向闭环                                                                                                                                                                                  |
| NFRs     | 纯客户端；双主题；Web/桌面各自记忆默认一致                                                                                                                                                                             |

## 4. 关键决策留痕

| 决策                | 结论                                                        | 依据                                 |
| :------------------ | :---------------------------------------------------------- | :----------------------------------- |
| 入口/持久化         | 日历内偏好 + localStorage（键 CALENDAR_WEEKSTART）          | 用户澄清 a（键名由用户指定）         |
| 范围边界            | 仅日历布局口径，任务页"本周"不动                            | 用户澄清 a（后端相对日期属另一契约） |
| 节假日              | 裁剪                                                        | 无可靠数据源（早前决策）             |
| 日期号尺寸（DEF-1） | 周视图对齐月视图 .cal-date（24×20/0.8125rem/450，状态同款） | 用户冒烟反馈"周偏大"                 |

## 5. 派发记录

- 前端 RD（naotodo-frontend-developer）：CAL-06 → DEF-1；开工前已读取并遵循 `.agents/prompts/frontend-developer.md` 角色提示词。

## 6. 变更记录

- 2026-09-06 CAL-06 `02a3ec3d` → 冒烟反馈日期号偏大 → DEF-1 `318a8488` → 用户复验+自行样式微调（不影响验收）→ 通过归档。
- 门禁：vp lint 0、vue-tsc 相关 0、vitest 302、webapp/desktopapp build 绿。

## 7. 遗留项与待重新评估

- **P1**：`calendar.weekstart_change` 埋点。
- **P2**：monthly-layout 新参数（weekStart/maxLanes）单测（待 web 测试基线）。
- 用户自行样式微调未纳入本归档 commit（如有需由用户另行提交）。