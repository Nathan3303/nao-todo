# 2026-09-14 子任务节点 UI 优化（TASK-06）

- **交付**：rd-fe 实现（`a250cd96`）+ 用户后续样式/图标微调（`3cc7ada2`）；随 v1.7.5 批次发布
- **终签**：✅ 通过（用户验收；不启用 QA）
- **关联**：TASK-04 子任务行标签展示的延续；STAT-01 领域统计属性（`checkItemCount` / `subtaskCount`）

## 1. 问题证据与 5 Whys

**现象（用户直提）**：任务详情面板子任务节点存在「脱离父任务」按钮冗余；描述与时间分行占用空间；缺少检查项/子任务数量提示。

| 层  | 追问                                                               |
| :-- | :----------------------------------------------------------------- |
| ①   | 子任务行显示冗余、信息密度低                                       |
| ②   | 脱离按钮低频且可通过其它路径实现；时间/描述分行占地                |
| ③   | 数量信息（检查项/子任务）未在子任务行暴露（数据已由 STAT-01 提供） |
| ④   | **命题：移除脱离按钮、时间与描述合并单行、名称行右侧增补数量徽标** |

## 2. 目标指标

| 项       | 口径                                      |
| :------- | :---------------------------------------- |
| 中间指标 | 子任务行信息密度提升（三行 → 两行主信息） |
| 成功口径 | 用户验收；vitest 全绿；双端 build ✓       |

## 3. 范围 / 非范围

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **做**   | ①移除 `.subtask-row__detach` 按钮 + `handleDetachSubTask` + `detachSubTask` 注入 + NueConfirm/NueMessage/translateTaskError import + 相关样式（保留 i18n `detachFromParent` 键）；②时间与描述合并单行：`[timeText, description].filter(Boolean).join(' · ')`，任缺剩一、皆空不渲染、无悬空分隔符，`clamped=2` + `title` 全文；③名称行右侧数量徽标：`checkItemCount`→icon `check`、`subtaskCount`→icon `connection`（复用 NueUI 图标），`v-if>0` 独立渲染，`flex: 0 0 auto` 防挤占 |
| **不做** | 不改时间文案/拼接规则；不删 i18n detach 键；不动移动端 `presentation-react`；不改其它任务详情组件                                                                                                                                                                                                                                                                                                                                                                                 |

## 4. 用户场景

- **场景卡**：打开带子任务的任务详情 → 子任务行显示「名称 + ✓n 徽标 + 标签栏 + ⤷n 徽标」+「时间 · 描述」单行。
- **关键路径**：详情面板 → 子任务列表 → 行内名称/徽标/摘要行。
- **页面状态清单**：有标签/无标签；有时间/描述/两者/皆无；数量为 0（不渲染徽标）；完成态（删除线）。

## 5. 业务规则

- 数量语义沿 STAT-01：`checkItemCount` 为检查项总数（含已完成）；`subtaskCount` 为直接子任务（1 层）。
- 数量为 0 不渲染。
- 徽标顺序：名称 → ✓徽标 → 标签栏 → ⤷徽标（用户后续调整，`3cc7ada2` 同步测试断言）。

## 6. NFRs

- 注入缺失降级（空串）；i18n 中英双语文案。
- 门禁：vp check 目标 pass + vitest 全绿（subtasks 17/17）+ 双端 build ✓。

## 7. AC（五覆盖）

| #   | 覆盖   | Given                                 | When         | Then                                        |
| :-- | :----- | :------------------------------------ | :----------- | :------------------------------------------ |
| AC1 | 主路径 | 打开详情                              | 查看子任务行 | 无脱离按钮；名称行含数量徽标；时间·描述单行 |
| AC2 | 边界   | 仅时间 / 仅描述 / 皆无                | 查看         | 各自正确显示，无悬空 `·`，皆无则不渲染      |
| AC3 | 空值   | `checkItemCount=0` / `subtaskCount=0` | 查看         | 对应徽标不渲染                              |
| AC4 | 截断   | 描述超两行                            | 查看         | 两行截断（`clamped=2`），`title` 提供全文   |
| AC5 | 工程   | 提交/发版                             | 执行         | 门禁全绿、双端 build、随 v1.7.5 发布        |

**不达标处置预案**：徽标顺序/截断异常 → 回退并复验 AC1–AC4。

## 8. 优先级

| 筛子     | 结论                                                             |
| :------- | :--------------------------------------------------------------- |
| 战略筛子 | 通过（用户直提、低成本）                                         |
| MoSCoW   | Should                                                           |
| Kano     | 期望型                                                           |
| RICE     | Reach 3 × Impact 2 × Confidence 90% / Effort 0.5 人天 ≈ **10.8** |

## 9. 决策留痕

- **脱离按钮**：一并移除逻辑（防死代码），保留 i18n 键（防其它引用）。
- **时间·描述合并**：单行字符串拼接（中圆点 `·` 分隔），保留 `clamped=2`。
- **徽标**：复用 NueUI 既有图标（不新增图标资源）；新增 i18n `task.details.checkItemCount` / `subtaskCount`（zh/en/types 三处同步）。
- **用户后续微调**（`3cc7ada2`）：✕ 徽标位置调整（check 徽标移至标签栏前）、`title-line gap 2xs→xs`、`meta line-height 1.4→1.5`、iconfont 重新生成；测试断言同步（名称 → check 徽标 → 标签栏 → subtask 徽标）。

## 10. 派发记录

| 任务                  | 会话  | commit                        | 结果 |
| :-------------------- | :---- | :---------------------------- | :--- |
| TASK-06 子任务节点 UI | rd-fe | `a250cd96`（+ 3 locale 文件） | ✅   |
| 用户样式 + 图标微调   | rd-fe | `3cc7ada2`                    | ✅   |

## 11. 验收结果

- 门禁：vp check 目标 pass；vitest 全绿（subtasks 17/17，含 3 例徽标）；双端 build ✓。
- 提交卫生：`a250cd96` 恰 5 文件（subtasks.vue + 测试 + locales×3）；`3cc7ada2` 恰 4 文件。
- 用户终签：✅ 通过（2026-09-14）。

## 12. 变更记录

- 2026-09-14：澄清 4 项定案 → rd-fe 实现 `a250cd96` → 用户样式微调 `3cc7ada2` → 随 v1.7.5 发布（`3626fd90` / tag `v1.7.5`）。

## 13. 遗留项

- 无。