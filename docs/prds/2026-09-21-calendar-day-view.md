# 2026-09-21 日历界面日视图（TASK-16）

- **状态**：PRD 定稿，**待用户开工确认**（澄清已完成 Q1–Q6；Q2 子问题与 Q7 未答，已按下述假设 A1/A2 固化）
- **关联**：`apps/web/src/components/calendar/`（monthly / weekly / aside）；上游单 TASK-08 日历排序、TASK-11 日历 UI、CAL-04 月/周双视图、CAL-07 拖拽改期
- **澄清来源**：用户 2026-09-21 答 Q1=A/B、Q2=B、Q3=a、Q4=a+a、**Q5=全做**、Q6=按推荐

## 0. 待确认假设（PM 明确声明，非静默猜测）

| #   | 假设                                                                                                      | 依据                                        |
| :-- | :-------------------------------------------------------------------------------------------------------- | :------------------------------------------ |
| A1  | **展示连续到分钟（不吸附）**；仅**交互**（拖拽/拉伸/点格新建）**吸附 30 分钟**                            | Q2 只答"B"，未答分钟落列子问；取 PM 推荐值  |
| A2  | 时间轴 **0–23 全时段展开**，不做折叠/滚动                                                                 | Q7 未答；取 PM 推荐值                       |
| A3  | 默认视图**仍为月**，不改变用户既有视图偏好行为                                                            | 未提及；最小改动                            |
| A4  | 时长为 0 的任务显示为**最小 1 列（30 分钟）宽**；**仅有 `startAt` 无 `endAt`** 的任务归**顶部「全天」行** | 需补规则；与 R1"不占格"略有出入，待架构确认 |
| A5  | 拖拽**允许跨日移动**（与月/周拖拽改期行为一致）                                                           | 与既有 F4 行为对齐                          |

## 1. 问题证据与 5 Whys

**现象（用户直提）**：日历只有月/周视图；周视图一天仅占 1/7 宽度，无法看清「几点做什么」，也无法做时间块规划。

| 层  | 追问                                                                   |
| :-- | :--------------------------------------------------------------------- |
| ①   | 用户想在一天内做时间块规划 / 看清具体时刻                              |
| ②   | 月/周视图横坐标是「日期」，天然无法表达时刻                            |
| ③   | 缺少以「时间」为横坐标的视图                                           |
| ④   | 且既有几何函数硬编码 7 列、打包函数未导出，无法直接复用                |
| ⑤   | **命题：新增日视图（横坐标=时间，30 分钟列，整点有文本、半点无文本）** |

## 2. 目标指标

| 项       | 口径                                                                     |
| :------- | :----------------------------------------------------------------------- |
| 中间指标 | 日视图可表达任务的实际时刻与时长（分钟级）；拖拽/拉伸/点格新建全链路可用 |
| 成功口径 | 用户实测可在日视图完成"看清 + 调整"；门禁全绿                            |

## 3. 范围 / 非范围

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| :------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **做**   | ① 三态切换新增 **日视图**（沿用现有切换控件）；②横坐标 = 48 个 **30 分钟**列，**偶数位（整点）显示文本、奇数位（半点）不显示文本**；③任务条按分钟**连续**定位（不吸附）；④纵向沿用**轨道 + `+N`** 溢出折叠；⑤跨日任务**裁到当日 + 两端续接标记**；⑥无时刻任务置于顶部**「全天」行**；⑦**当前时间指示线**（仅锚点日=今天时显示）；⑧**点空白格快速新建**（带时间，吸附 30 分钟，默认时长 30 分钟）；⑨**水平拖拽改时间**（吸附 30 分钟，保持时长）；⑩**右边缘拉伸改时长**（吸附 30 分钟，最小 30 分钟）；⑪改期/拉伸并入既有 **U2 撤销**；⑫i18n 中英；⑬沿用排序下拉（纵向顺序） |
| **不做** | 不做时间轴折叠/滚动（A2）；不做冲突检测与告警；不做精确分钟文本输入；不改数据结构与服务端契约；不改月/周视图既有行为；**不动** `presentation-react` / `apps/mobileapp`（移动端红线）；**不删**既有日内抽屉（Q6）                                                                                                                                                                                                                                                                                                                                                            |

## 4. 用户场景

- **画像**：需要按时刻安排/检查一天任务的用户（时间块规划）。
- **场景卡**：进日历 → 切「日」→ 看到 0–23 时间轴与当天任务条 → 拖动任务条改到别的时段 → 在空白处点一下新建 30 分钟任务 → 拖右边缘拉长到 1 小时 → 看错可撤销。
- **关键路径**：三态切换 → `viewMode='day'` → `buildDayGrid(anchorKey, tasks, …)`（分钟级 span → 列/轨道）→ 渲染时间轴 + 任务条 → 交互（新建/拖拽/拉伸）→ `taskUseCase.update({startAt,endAt})` → 快照联动重渲染。
- **页面状态清单**：加载中 / 加载失败（含重试）/ 空态（当日无任务）/ 有任务 / 锚点日=今天（时间线可见）/ 非今天（时间线隐藏）/ `+N` 折叠态 / 拖拽进行中 / 撤销条可见。
- **边界数据**：时长为 0；时长为 1 分钟；时长 ≥ 1 天（跨日）；仅 `startAt`；仅 `endAt`；无任何时间；开始/结束跨午夜；同一时段 ≥ 5 个任务（`+N`）；`14:37` 这类非 30 分钟整数分钟。

## 5. 业务规则

### 5.1 列模型与头部

- **48 列**，每列 30 分钟（`00:00`–`23:30`）。
- 列头：列索引为**偶数**（对应整点 `HH:00`）显示文本 `HH`；列索引为**奇数**（`HH:30`）**不显示文本**（仅刻度视觉）。首列起于 `00:00`。
- 周起始口径 `weekStart` 对日视图**不适用**。

### 5.2 任务条定位（分钟级，连续）

- 仅当 `startAt` 与 `endAt` **均有效**时进入时间轴：
    - `startMin` = 当日内的分钟偏移（钳制到 `[0, 1440]`）；`endMin` 同理；**跨日 = 裁剪到当日**。
    - `left = startMin / 1440`，`width = max(endMin - startMin, MIN_SPAN_MIN) / 1440`，其中 `MIN_SPAN_MIN = 30`（A4：时长为 0 时最小 1 列宽）。
    - **不吸附**：`14:37` 起 → 条从 14:37 处起（位于 `14:30` 与 `15:00` 两列之间）。
- 续接标记：任务起始早于当日 → 左端续接；结束晚于当日 → 右端续接（与周视图 `cont-start`/`cont-end` 同款语义）。

### 5.3 全天行与归属

- 顶部**「全天」行**（独占一条，不参与时间轴与 `+N`）：收录满足任一者——`endAt` 有效但 `startAt` 无效（R1：归属截止日当日）；仅 `startAt` 有效（A4）；两者皆无（不占任何日期，不入日视图）。
- 跨日任务的**当日裁剪**：若裁剪后覆盖 `00:00–24:00` 整天，则**进入全天行**而非时间轴（避免出现占满整行的长条）。

### 5.4 纵向与溢出

- 纵向沿用**轨道（lane）** 模型：同一时段的多个任务纵向堆叠；超出可视轨道数的部分折叠为 `+N`（触发点与既有 `+N` 一致的交互：点开查看）。
- 轨道分配沿用既有贪婪首次适配；主键为**开始分钟升序**（对应既有的"起始列升序"），同起点内按**用户排序**（TASK-15 口径）。
- 可视轨道数由行高实测（复用 `useCalendarGrid` 的 DEF-2 逻辑）。

### 5.5 交互

| 交互       | 触发            | 规则                                                                                                       |
| :--------- | :-------------- | :--------------------------------------------------------------------------------------------------------- |
| 快速新建   | 点时间轴空白处  | `startAt` = 点击时刻**向下吸附到 30 分钟**；`endAt` = startAt + 30 分钟；复用既有 quick-create 流程与 i18n |
| 拖拽改时间 | 拖任务条主体    | 横向位移映射为分钟；**吸附 30 分钟**；**时长不变**；允许跨日（A5）；拖动中显示落位提示；松手提交           |
| 拉伸改时长 | 拖任务条右边缘  | 只改 `endAt`；**吸附 30 分钟**；**最小 30 分钟**；不允许 `endAt < startAt`                                 |
| 撤销       | 既有 U2 撤销条  | 改期/拉伸并入既有撤销栈（与月/周 F4 一致）                                                                 |
| 打开详情   | 点任务条        | 复用既有 `openTaskDetails`                                                                                 |
| 当前时间线 | 仅锚点日 = 今天 | 竖向红线 + 顶部圆点；每 **30 秒**更新一次；非今天不渲染                                                    |

### 5.6 时序与一致性

- 交互提交后不本地乐观改写时长以外的字段；成功后由商店快照联动重渲染（与月/周一致）。
- 排序下拉（TASK-08/TASK-15）对日视图纵向生效，与月/周同源。

## 6. NFRs

- **几何参数化**：任务条定位函数必须接受**列数**参数（现 `segmentStyleOf` 硬编码 7 列），月/周/日三视图共用同一实现，禁复制第二套几何。
- **打包真源唯一**：轨道分配 + `+N` 溢出必须**抽取复用**既有 `buildRowContent` 逻辑（现为模块私有），禁在日视图另写一套。
- 逐帧性能：拖拽/伸缩期间**不触发数据刷新**，仅本地位移；松手才提交；48 列 + 多轨道下无可见卡顿。
- 纯函数可单测（列模型、分钟映射、吸附、裁剪、续接、`+N`）。
- 失败路径：提交失败 → toast + 回退到提交前位置（不留乐观脏状态）。
- 门禁：`vp test` 全绿、`vp check` 全绿、`guard:ddd` OK、双端 build ✓。

## 7. AC（五覆盖）

| #   | 覆盖      | Given                                                                    | When                                                         | Then                                                                                                                                                             |
| :-- | :-------- | :----------------------------------------------------------------------- | :----------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC1 | 主路径    | 锚点日有「09:00–10:30」任务，视图切到「日」                              | 渲染                                                         | 头部 48 列、整点有文本、半点**无文本**；该条起于 09:00 列、跨 3 列（1.5 小时）；纵向轨道与月/周一致                                                              |
| AC2 | 分钟连续  | 任务为「14:37–15:07」                                                    | 渲染                                                         | 条起于 14:37 位置（位于 14:30 与 15:00 之间）、宽 30 分钟；**不被吸附**到 14:30/15:00                                                                            |
| AC3 | 边界      | ①时长 0 ②跨日（昨 22:00–今 02:00）③跨满全天 ④仅 `endAt` ⑤同时段 6 个任务 | 渲染                                                         | ①最小 30 分钟宽 ②裁到 00:00–02:00 且左端续接 ③进**全天行** ④进全天行 ⑤显示可视轨道 + `+N`                                                                        |
| AC4 | 交互      | 日视图有任务 A(09:00–10:00)                                              | ①拖到 13:10 ②拉伸右边缘到 11:07 ③点 15:20 空白处 ④执行后撤销 | ①`startAt=13:00, endAt=14:00`（吸附 30 分且时长不变）②`endAt=11:00`（吸附、≥30 分钟）③新建 `15:00–15:30` ④恢复原值                                               |
| AC5 | 负向+工程 | ①提交失败 ②锚点日=今天 ③非今天                                           | ①拖拽松手 ②③渲染                                             | ①toast + 位置回退、无脏状态 ②显示当前时间线且 30 秒更新 ③不显示时间线；且 `vp test`/`vp check`/`guard:ddd`/双端 build 全绿、几何与打包各仅一处实现、移动端零改动 |

**不达标处置预案**：列头文本位置错 → 修列模型并补单测；分钟映射偏差 → 修 `left/width` 公式并补 `14:37` 类用例；出现第二套几何/打包实现 → 回退重构后再进；拖拽期间触发刷新 → 改本地位移 + 松手提交。

## 8. 优先级

| 筛子     | 结论                                                                                        |
| :------- | :------------------------------------------------------------------------------------------ |
| 战略筛子 | 通过（用户直提；补齐日历视图维度）                                                          |
| MoSCoW   | Should                                                                                      |
| Kano     | 期望型                                                                                      |
| RICE     | Reach 3 × Impact 3 × Confidence 80% / Effort 5 人天 ≈ **1.44**（工作量偏大，故拆 4 单交付） |

## 9. 决策留痕

- **Q1（用户）**：动机 = 周视图一天太窄看不清时刻（A）+ 时间块规划（B）。
- **Q2（用户）**：**方案 B · 48 列**（30 分钟/列，偶数位整点有文本、奇数位半点无文本）。分钟落列子问未答 → **A1**。
- **Q3（用户）**：纵向沿用**轨道 + `+N`**。
- **Q4（用户）**：跨日**裁到当日 + 续接标记**；无时刻任务**顶部「全天」行**。
- **Q5（用户）**：**全做**（只读 + 时间线 + 快速新建 + 拖拽改时间 + 拉伸改时长）→ 故拆 T50（只读渲染）与 T51（交互）两单，避免单点过大。
- **Q6（用户"按推荐"）**：三态切换沿用现有控件；**保留**既有日内抽屉（`day-drawer`）——点日期看清单、点时间轴看时刻，互补。
- **Q7（未答 → A2）**：0–23 全时段展开。
- **A3**：默认视图仍为月。
- **A4/A5**：见 §0，待架构评审确认（尤其 A4 与 R1 的"仅 startAt 不占格"存在张力）。

## 10. 派发记录

| 任务 | 目标会话      | 角色 | 概要                                                              | 对应 AC | 状态   |
| :--- | :------------ | :--- | :---------------------------------------------------------------- | :------ | :----- |
| T48  | arch-designer | 架构 | 列模型参数化 / 打包抽取 / 分钟吸附与跨日的领域边界（A4/A5）       | —       | 待派发 |
| T49  | qa            | 测试 | 用例先行（列头 / 分钟映射 / 裁剪 / 全天行 / 吸附 / 交互 / `+N`）  | AC1–AC5 | 待派发 |
| T50  | rd-fe         | 前端 | 只读渲染：48 列时间轴 + 分钟级任务条 + 全天行 + 跨日裁剪 + 时间线 | AC1–AC3 | 待派发 |
| T51  | rd-fe         | 前端 | 交互：点格新建 + 拖拽改时间 + 拉伸改时长 + 撤销并入               | AC4     | 待派发 |
| T52  | qa            | 测试 | 独立验收（含双端 build / 红线 / 拖拽性能）                        | AC1–AC5 | 待派发 |

## 11. 验收结果

—

## 12. 变更记录

- 2026-09-21：用户提出日视图（横坐标=时间小时节点 + 节点间 30 分钟中间值且头部不展示文本）→ grill-me 主干澄清（Q1–Q7）→ 本 PRD 初稿。

## 13. 遗留项

- A1/A2/A4/A5 四项假设待用户在开工确认时确认或改写。
- 冲突检测与告警（Q1-c）未纳入，前置假设是用户未选；如需要另立单。
- 精确分钟文本输入、时间轴折叠/滚动、移动端日视图均不在本单。