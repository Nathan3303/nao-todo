# 2026-09-14 子任务标签栏 flex 修复 + oxfmt 归一化 + .agents 重组（v1.7.4）

- **交付**：用户手动修复（subtasks.vue）+ rd-fe 执行（oxfmt 归一化 / .agents 重组 / 发版）
- **终签**：✅ 通过（用户指示发版 v1.7.4；T3/T4 门禁全绿）
- **关联**：TASK-04 后续样式修复；全仓格式 backlog 清理（此前 843 文件 backlog）；`.agents` 工具链目录重组

## 1. 问题证据与 5 Whys

**现象（三组独立变更，用户指示一并发版）**：

| #   | 变更                       | 证据                                                                                                                                        |
| :-- | :------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 子任务标签栏 flex 压缩问题 | TASK-04 交付后用户实测发现 TaskTagBar 在名称/标签共存时 flex 收缩异常（标签被压缩/名称挤压）                                                |
| 2   | 全仓 oxfmt 格式 backlog    | 此前多轮发版以 `--no-verify` 规避 md oxfmt 重排，积累 843 文件格式 backlog（实际以代码为主：546 ts/133 vue/20 tsx/47 css/49 json/45 md 等） |
| 3   | `.agents` 目录重组         | prompts/common/scripts 重构 + 技能包拆分；旧 commands/grill-me/nue-ui 技能删除                                                              |

| 层  | 追问                                                                                                                   |
| :-- | :--------------------------------------------------------------------------------------------------------------------- |
| ①   | 标签栏显示异常（flex 压缩） / 仓库格式未归一 / .agents 结构陈旧                                                        |
| ②   | TASK-04 布局后续暴露 flex 收缩缺陷；oxfmt 从未全仓跑过；.agents 命令/技能与现行工作流脱节                              |
| ③   | 修复需精调 gap/nowrap/flex 基值；归一化需全仓执行并验证零逻辑变更；重组需拆技能包                                      |
| ④   | **命题：用户手动修复样式（commit B）+ rd-fe 执行 oxfmt 全仓归一化（commit C）+ .agents 重组（commit A），发版 v1.7.4** |

## 2. 目标指标

| 项       | 口径                                                                                                            |
| :------- | :-------------------------------------------------------------------------------------------------------------- |
| 中间指标 | oxfmt 全仓零语义变化（`git diff -w` 仅空白/换行）；vp check 全绿无循环                                          |
| 成功口径 | 三 commit 提交 + v1.7.4 发版（root/webapp/desktopapp 1.7.4、presentation 0.4.3）；门禁全绿；`.agents/` 变更并入 |

## 3. 范围 / 非范围

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                        |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **做**   | ① `subtasks.vue` 手动修复：`.subtask-row__title-line` gap `xs→2xs` + `flex-wrap: nowrap`；`.subtask-row__tags` 移除 `max-width:55%`、`flex: 0 1 auto→0 0 auto` + `nowrap`（TaskTagBar flex 压缩修复）；② oxfmt 全仓归一化（`vp check --fix`，含换行符对齐 `.editorconfig` CRLF，纯格式零逻辑）；③ `.agents` 全部变更（D/M/?? 24 条）纳入提交；④ 发版 v1.7.4 |
| **不做** | 不改业务逻辑/断言/配置语义；不发版 v1.7.4 之外版本；不清理 `.agents` 外其他未跟踪文件；移动端红线 `presentation-react` 仅格式、零语义改动                                                                                                                                                                                                                   |

## 4. 用户场景

- **场景卡**：用户实测发现标签栏 flex 压缩 → 手动修复 → 指示一并归一化 + 提交所有变更（含 `.agents`）→ 发版 v1.7.4。
- **关键路径**：工作树 3 组变更 → rd-fe 提交 → 发版。
- **页面状态清单**：修复后子任务行名称/标签栏 flex 正常（`nowrap` 防压缩）。

## 5. 业务规则

- 版本号显示、`.agents` 为工具链内部，不影响业务逻辑。
- 归一化纯格式：`git diff -w` 验证零语义变化；`vp check` 复跑无循环未收敛。
- 换行符按 `.editorconfig`（`[*] end_of_line=crlf`）统一。

## 6. NFRs

- 零逻辑变更（`-w` 验证）；门禁：vp check 1219 文件全对 + lint/type 1045 文件零错误 + vitest 645 例全绿 + 双端 build ✓。

## 7. AC（五覆盖）

| #   | 覆盖     | Given           | When         | Then                                                  |
| :-- | :------- | :-------------- | :----------- | :---------------------------------------------------- |
| AC1 | 主路径   | 工作树 3 组变更 | 提交+发版    | v1.7.4 发版成功、tag 推送                             |
| AC2 | 零语义   | oxfmt 归一化    | diff -w 验证 | 代码文件零语义变化（仅 docs md 表格对齐 + JSON 压缩） |
| AC3 | 提交纯净 | 三范围          | 核对         | commit A/B/C 各自干净、`.agents/` 全入、无夹杂        |
| AC4 | 负向闭环 | 修复后          | 回归         | vitest 645 全绿（subtasks 14/14）；双端 build ✓       |
| AC5 | 一致性   | 终态            | git status   | 干净、分支与远端同步、无残留                          |

**不达标处置预案**：归一化出现循环未收敛 → 报告并停手，不强制过断言。

## 8. 优先级

| 筛子     | 结论                                                                      |
| :------- | :------------------------------------------------------------------------ |
| 战略筛子 | 通过（用户直提三组变更一并发版）                                          |
| MoSCoW   | Should（修复 + 技术债清理 + 工具链重组）                                  |
| Kano     | 期望型（用户实测触发的修复）                                              |
| RICE     | Reach 3 × Impact 3 × Confidence 90% / Effort 1 人天 ≈ **8**（patch 批次） |

## 9. 决策留痕

- **修复归属**：subtasks.vue 为用户手动修复（TaskTagBar flex 压缩），独立 commit B（style），不并入 oxfmt 格式 commit。
- **版本矩阵**：root/webapp/desktopapp `1.7.3→1.7.4`（每版协同 bump）；presentation `0.4.2→0.4.3`（B commit 真实样式修复，沿 v1.7.1 先例）；shared/infrastructure/presentation-identity/domain-task/presentation-react 不动（C 纯格式零语义，不触发语义 bump）。
- **归一化范围**：实际以代码为主（546 ts/133 vue/20 tsx/47 css/49 json/45 md + 换行符 CRLF 对齐）；oxfmt 未动 subtasks.vue（提交前后哈希一致，B 纯净）。
- **`.agents` 全量并入**：用户明确指示「提交所有变更文件，包括 .agents」，commit A 含 D/M/?? 25 文件。

## 10. 派发记录

| 任务                       | 会话                | commit                                                       | 结果            |
| :------------------------- | :------------------ | :----------------------------------------------------------- | :-------------- |
| T3 oxfmt 归一化 + 全量提交 | rd-fe（`01a09ddb`） | `4b7eae3`（agents）/ `b0db5d6`（style）/ `764044c`（format） | ✅ 完成，门禁绿 |
| T4 发版 v1.7.4             | rd-fe               | `070625b6` + tag `v1.7.4`                                    | ✅ 完成，已推送 |

## 11. 验收结果

- 门禁（rd-fe 实测）：vp check 1219 文件格式全对 + lint/type 1045 文件零错误（复跑 exit 0 无循环）；vitest 73 文件 / 645 例全绿（subtasks 14/14）；webapp build 14.55s / desktopapp build 17.90s。
- `git diff -w`：829 文件仅空白/换行差异；仅 30 docs 有空白之外 diff（md 表格分隔行对齐 + 证据 JSON 数组压缩），语义零变化。
- 提交卫生：A 25 文件（12A/10D/3M）、B 1 文件（4+/4-）、C 829 文件（+50163/-50609）；`.agents/` 全入、未卷入范围外文件。
- 发版：`070625b6` 恰含 5 文件（CHANGELOG + root/web/desktop/presentation 4 package.json）；tag `v1.7.4` peel 一致、已推送。
- 用户终签：✅ 指示发版 v1.7.4（2026-09-14）。

## 12. 变更记录

- 2026-09-14：用户手动修复 subtasks.vue → 指示 oxfmt 归一化 + 全量提交（含 `.agents`）→ T3 提交三 commit → 发版 v1.7.4（`070625b6` + tag）。
- 发版链：v1.7.3（TASK-05 版本号显示）→ v1.7.4（本单）。

## 13. 遗留项

- 无（本批次全部交付闭环；oxfmt 归一化已把 md 基线统一，未来 PRD/ADR 提交无需 `--no-verify`）。