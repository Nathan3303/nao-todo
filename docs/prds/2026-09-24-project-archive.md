# 2026-09-24 清单归档 PRD（归档 = 收起但可找回；闭环 = 查看 / 恢复 / 单任务脱归档）

> **状态**：**待用户确认开工**（本 PRD 已含用户 12 条口径；技术口径待 arch 评审 ⇒ 见 §12）。
> **依据**：现状核查报告 `docs/reports/2026-09-24-project-archive-status.md`（`T174` 探针，**commit `ce272bc3`**）+ 用户 2026-09-24 逐条决策（ask_me 7 项 + 追加 2 条规则）+ 业界对照（§10）。
> **硬约束**：**不丢失数据**（归档绝不删数据）· **不丢失功能**（阶段一/二零回归）· **移动端零改动**。
> **前置**：**`DEF-36` 必须与入口同批修复** —— `ProjectUseCase.archive()` / `unarchive()` 当前均误调 `projectRepo.delete()`（潜在 P0：一旦放开入口即**误删清单并级联删任务**）。

## 1. 问题证据（5 Whys）

| 层       | 内容                                                                                                                                                     |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 现象     | **清单无归档可用**：web/desktop 无归档入口（菜单项被注释）；即便触发，`archive()` 实为 `delete()`                                                        |
| Why1     | 归档的**数据骨架早已铺好**（`Project.archivedAt` + 本地库字段 + 同步字段双向闭环 + 服务端 `Archive`/`Unarchive` API 与**双向级联**），但 UI/闭环从未接线 |
| Why2     | 归档入口被注释掉、`archive/unarchive` 方法体写成 `delete` 占位 ⇒ **半成品**（探针定性：「骨架齐备、闭环缺失、核心实现是错的」）                          |
| Why3     | 无「已归档清单」查看/恢复入口 ⇒ 即使用户能归档也**找不回**（`unarchive` 目前**仅服务端 API 可达**，i18n/组件/store action 全是死件）                     |
| **根因** | **缺少「归档 = 收起但可找回」的完整用户闭环**，且**缺少「归档清单内任务如何处置」的可见性规则**（客户端无级联 ⇒ 离线归档后任务仍留在日历/看板）          |

## 2. 目标指标

| 指标         | 目标           | 说明                                                                     |
| :----------- | :------------- | :----------------------------------------------------------------------- |
| 归档可逆性   | **100%**       | 归档清单与任务**均可完整恢复**，恢复后回原位                             |
| 数据零丢失   | **0 删除**     | 归档路径**不得**触碰 `deletedAt`（`DEF-36` 修复为前置）                  |
| 可见性一致性 | **7/7 处排除** | 侧栏清单 / 搜索 / 日历 / 看板 / 统计计数 / 番茄选择器 / 新建任务清单下拉 |
| 两端一致     | **100%**       | web 与 desktop 行为一致（同组件同逻辑；移动端零改动）                    |
| 离线一致     | **100%**       | 离线归档/恢复成功、级联一致、联网自动回传                                |

## 3. 范围 / 非范围

**范围（本批）**

1. **入口**：清单头部操作菜单 + **清单右键菜单**（两处一致）⇒「归档清单」；**二次确认**（提示「该清单内 N 个任务将一并归档」）
2. **归档语义**：整清单归档 ⇒ 清单从活动区收起；**任务随清单一起隐藏/恢复**（客户端**同级联** + 服务端既有级联；离线一致）
3. **查看与恢复**：侧栏清单区底部**「已归档」入口** ⇒ 面板列出已归档清单（名称 / 任务数 / 归档时间）⇒ **「取消归档」** ⇒ 清单**回侧栏原位**、该清单仍归档的任务**全部恢复**
4. **归档清单内只读**：禁止新建/编辑/删除/改期/移动/番茄等**一切变更**；**唯一例外 = 逐条「取消归档」**（见 5）
5. **单任务脱归档（用户追加规则）**：归档清单内可对**单条任务**「取消归档」⇒ 因清单仍归档，该任务**归属「收集箱」** + **可见提醒**（中英）；若清单已恢复 ⇒ 该任务回到原清单
6. **可见性**：归档清单与归档任务在上述 **7 处全部排除**；**搜索默认排除**，并提供**显式开关「包含已归档」**（开时命中并带「已归档」标识）
7. **禁归档内建清单**：收集箱等内建清单**不可归档**（入口不出现或禁用 + 兜底拒绝）

**非范围（本批不做，显式登记）**

- **单任务归档入口**（本批只做「归档清单」+「单任务**取消**归档」；是否需要「单任务归档」待用户在开工确认卡拍板）
- **自动归档**（按时间/完成率等策略）· **归档统计/报表** · 归档清单的**永久删除**（仍走现有「删除清单」）
- **批量**取消归档（面板内可后续增强）· **移动端任何改动**（红线）
- **字段级/复杂恢复**（如把归档任务恢复到指定清单）

## 4. 用户场景

| #   | 场景                                       | 期望                                                                                                    |
| :-- | :----------------------------------------- | :------------------------------------------------------------------------------------------------------ |
| S1  | 一个清单做完了，想收起来但不想删           | 归档 ⇒ 二次确认后从侧栏消失，任务不再出现在日历/搜索/看板/统计/番茄/新建下拉                            |
| S2  | 两周后想起某个已归档清单                   | 侧栏底部「已归档」⇒ 面板可见（含任务数/归档时间）⇒「取消归档」⇒ 回原位、任务全恢复                      |
| S3  | 只想把归档清单里的**某一条**任务捡回来干活 | 打开归档清单 ⇒ **只读查看** ⇒ 对该任务「取消归档」⇒ 任务**进入收集箱** + **明确提醒**（因为清单仍归档） |
| S4  | 离线时归档/恢复                            | 本地立即生效（级联一致），联网自动回传；冲突按 2B 的 OCC/journal 机制处理                               |
| S5  | 搜索里想找归档内容                         | 默认搜不到；打开「包含已归档」开关后可命中，结果带「已归档」标识                                        |

## 5. 业务规则

1. **归档 ≠ 删除**：归档只置 `archivedAt`，**绝不**写 `deletedAt`；删除仍走现有「删除清单」流程。
2. **级联**：清单归档 ⇒ 其下任务一并归档（客户端本地级联 + 服务端既有级联，双端一致）；清单恢复 ⇒ 其下**仍处于归档状态**的任务一并恢复。
3. **只读**：清单处于归档态 ⇒ 其下任务**只读**（禁一切变更）；**允许**的唯一动作 = 单任务「取消归档」。
4. **单任务脱归档的归属（用户规则）**：任务取消归档时若**其清单仍归档** ⇒ 任务改为**归属收集箱**并**提醒用户**；若清单已恢复 ⇒ 任务回到原清单。（⚠️ 收集箱的表示沿用既有约定 —— 见 §12 待裁定 ④。）
5. **可见性**：归档清单/任务在 7 处默认排除；搜索默认排除 + 显式「包含已归档」开关。
6. **复位**：取消归档后清单回侧栏**原位**（保留 `sortId`，探针已确认数据层可行）；任务恢复后回到原清单原位置。
7. **二次确认**：归档前列出「N 个任务将一并归档」；取消 = 不归档（无副作用）。
8. **内建清单不可归档**（收集箱等）。

## 6. NFRs（业务视角）

- **数据完整性**：归档/恢复**不得**产生静默丢失；离线窗口内级联必须一致（不得出现「清单收起但任务仍可见」）。
- **可观测**：单任务脱归档 ⇒ 收集箱 + **可见提醒**；批量归档 ⇒ 确认框内数量可见。
- **性能**：归档/恢复为本地优先写（即时），不得因归档查询拖慢列表（7 处过滤须为既有查询的谓词扩展，不引入全表扫描）。
- **一致性**：web/desktop 同组件同逻辑；移动端零改动。
- **可回归**：阶段一（离线镜像/迁移门/清库/明文姿态/状态展示）与阶段二（OCC/冲突 UX/多标签/偏好）**零回归**。

## 7. AC（五覆盖）

| AC      | 覆盖        | Given / When / Then                                                                                                                                                                                                    |
| :------ | :---------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **AC1** | 主路径      | **G** 活动清单 ／ **W** 头部菜单/右键「归档清单」+ 确认 ／ **T** 清单从侧栏消失、任务从 **7 处**消失；**可离线**执行并回传（`archivedAt` 双向）                                                                        |
| **AC2** | 主路径      | **G** 侧栏底部「已归档」／ **W** 打开面板 + 「取消归档」／ **T** 清单**回原位**、其下仍归档任务**全部恢复**；面板条目消失                                                                                              |
| **AC3** | 边界        | **G** 归档清单 ／ **W** 任意变更操作（新建/编辑/删除/改期/移动/番茄）／ **T** **一律不可**（只读）；**唯一例外** = 单任务「取消归档」可用                                                                              |
| **AC4** | 负向闭环    | **G** 归档清单内的某任务 ／ **W** 「取消归档」且**清单仍归档** ／ **T** 任务**归属收集箱** + **可见提醒**（中英）；且该任务此后可正常编辑                                                                              |
| **AC5** | 设计一致性  | **T** web/desktop 一致；**收集箱不可归档**；归档清单 `sortId` 保留（恢复回原位）；搜索默认排除 + 「包含已归档」开关命中带标识；阶段一/二**零回归**；移动端 **0 改动**                                                  |
| **AC6** | 契约 / 数据 | **T** `archive`/`unarchive` **不再**触碰 `deletedAt`（`DEF-36` 修复）；`archivedAt` 在 pull/push 双向闭环；离线性/恢复性一致；`DEF-12` 服务端口径（`isArchived=false` 空操作）**不被本批放大**（或同批修正，见 §12 ⑤） |

## 8. 上线闭环

| 环节             | 内容                                                                                                                                         |
| :--------------- | :------------------------------------------------------------------------------------------------------------------------------------------- |
| **前置**         | `DEF-36` 修复（archive/unarchive 误调 delete）+ 缺陷池 `DEF-12` 口径更正                                                                     |
| **架构评审**     | arch：§12 六项待裁定（客户端级联 / 7 处过滤补漏 / 三态边界 / 收集箱表示 / 只读面板路由 / 复位）                                              |
| **实现**         | 入口 + 二次确认 · 归档/恢复 + 级联 · 「已归档」面板 · 只读面板 + 单任务脱归档→收集箱 + 提醒 · 7 处可见性 + 搜索开关 · i18n 中英 · 清死件     |
| **回归（关键）** | 全范围 8 项门禁 + **阶段一/二回归矩阵** + **移动端 0**                                                                                       |
| **风险**         | R-A **误删数据**（若 `DEF-36` 未修即接线，P0）· R-B 离线级联不一致 · R-C 可见性漏处（看板/内置视图）· R-D 收集箱归属语义不明导致「任务消失」 |

## 9. 变更治理

- ADR：arch 出（归档/停用/删除三态边界 + 可见性谓词归属 + 与 OCC 的关系）
- 本 PRD 变更记录：v0.1（2026-09-24，PM 初稿，含用户 12 条口径）

## 10. 业界对照（设计依据）

| 产品        | 容器级联                                        | 默认视图 | 搜索                                                                   | 找回/恢复                                                       |
| :---------- | :---------------------------------------------- | :------- | :--------------------------------------------------------------------- | :-------------------------------------------------------------- |
| **Jira**    | 归档 issue（项目归档属管理动作）                | 排除     | **排除**（仅直链可达）                                                 | `Issues > Archived issues` 专用页 + Restore，**恢复到原始状态** |
| **Trello**  | **归档清单 ⇒ 清单内卡片一起归档**（可整体恢复） | 排除     | **默认包含**（需 `is:open` / 高级搜索过滤排除）                        | 看板「Archived items」+ 恢复                                    |
| **Asana**   | 归档项目                                        | 侧栏排除 | **保留可搜**（官方口径「仍可搜索」；社区反映高级搜索无法排除归档项目） | 侧栏 Projects 下拉归档区                                        |
| **Todoist** | 归档项目                                        | 侧栏排除 | **包含**（用户在社区求「能否不出现在搜索」）                           | 「显示已归档项目」入口                                          |

**业界共识（本 PRD 采纳）**：① 归档 ≠ 删除、**可逆**；② 默认视图/选择器一律不出现；③ **必须有专用「已归档」入口 + 恢复**；④ 恢复**回原位**；⑤ **容器归档会连内容一起收**（与服务端既有级联一致）；⑥ 归档态**只读**；⑦ 归档不参与统计/提醒/番茄。
**搜索一列为两派** —— 本 PRD 采纳「**默认排除 + 显式开关**」（用户 2026-09-24 确认「按建议」）：日常不打扰，同时保留「可找回」的归档价值。

## 11. 优先级（RICE 初评）

| 项           | Reach    | Impact                          | Confidence     | Effort                                | RICE     | 结论                 |
| :----------- | :------- | :------------------------------ | :------------- | :------------------------------------ | :------- | :------------------- |
| 清单归档闭环 | 全体用户 | 中高（清单生命周期收尾 + 找回） | 高（骨架已备） | 中（入口/面板/级联/可见性/只读/提醒） | **中高** | **Must（独立批次）** |

## 12. 待 arch 转译的技术项（架构评审闸门）

1. **客户端任务级联**：是否在任务仓储新增 archive/unarchive（与本地事务内级联）以对齐服务端？否则离线窗口内出现「清单收起但任务仍在日历」。
2. **可见性补漏**：看板查询 + **内置视图**（所有任务/今日/…）补 `isArchived` 过滤；统计计数（`taskCount`）口径是否含归档。
3. **三态边界**：`deactivedAt`（VO `isDeleted` 判据）vs `deletedAt`（本地 delete 写入）**语义不一致** ⇒ 需定义「归档 / 停用 / 删除」的权威判据与字段。
4. **「归属收集箱」的表示**：沿用 `projectId: ''`（CAL-03 约定）还是内建 inbox id？服务端 `task update projectId` 语义对齐。
5. **`DEF-12`**：服务端 `isArchived=false` 空操作（`scopes.go:139-146`）+ `sort` 无白名单 —— 本批是否同批修正（跨仓）。
6. **只读面板落点与复位**：归档清单详情用什么路由/组件（复用现有清单视图 + 只读守卫？）· `sortId` 复位策略（`resortWithRebuild` 是否计入归档项）。