# 桌面端（Electron）人工冒烟清单（TASK-25 阶段一.5）

> **目的**：真实 Electron 实机的人眼验收（机器不可判项）。权威来源：`docs/adr/2026-09-10-shell-02-sync-status-rail-teleport.md`（C1–C16 / AC-01…AC-14）· `docs/adr/2026-09-23-two-end-sync-status-unification.md`（D-3/D-4/D-6/D-7）· `docs/prds/2026-09-23-web-offline-stage1.md`（AC8/AC9/AC10/AC13b/AC17）· qa 报告 `docs/qa/2026-09-23-two-end-sync-status-unification-acceptance.md` §十二。
> **基线**：HEAD `e9150d96`（T115b 实现 `a31660f8` + T117 复核 `eeb7330a`）。

## 0. 前置

| 项         | 要求                                                                                                                          |
| :--------- | :---------------------------------------------------------------------------------------------------------------------------- |
| 后端       | `localhost:3302` 存活（`curl localhost:3302/api/ping` → `{"code":200}`）—— **已实测存活 ✓**；后端未起会冷启动白屏（既有缺陷） |
| 启动       | `pnpm desktop:dev --noSandbox`（或 `pnpm desktopapp dev --noSandbox`）；**改过 CSP/配置后须重启**（配置期解析，不热更）       |
| 账号       | 用你自己的 dev 账号登录；**不要把凭据写进仓库**                                                                               |
| 图形会话   | 需真实显示且**窗口能置前台**（窗口被遮挡时 Chromium 可能不产生渲染帧 ⇒ 观察失真）                                             |
| 可选自动化 | `node scripts/electron-smoke/run.mjs --launch`（自动起 Electron + CDP）；`--list` 看分组；`--only smoke-1,smoke-2` 只复跑     |

---

## A. 本次新增（TASK-25）—— **重点**

| #       | 操作步骤                                       | 判断点（预期）                                                                                                                                                    | 来源              |
| :------ | :--------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------- |
| **A1**  | 登录后看**内容区顶部**                         | **不得**出现任何条：「已更新」/「离线模式 · 数据截至 X」/「尚未同步完成」/「正在加载更多」/「已加载 N 条」—— 内容区第一行直接是页面内容                           | D-3 / D-7 / D-8   |
| **A2**  | 看左侧 70px 轨道**最底部**                     | 有**刷新图标按钮**，位于**齿轮上方**、齿轮恒最底                                                                                                                  | AC-07             |
| **A3**  | 在线、数据完整时看按钮                         | **无圆点**；悬停 tooltip 为「同步」（常态文案**逐字不变**）                                                                                                       | D-4 ③④ / AC-12    |
| **A4**  | **断网**（关 WiFi 或 DevTools offline）后重载  | 按钮出现**警告色（橙）实心圆点**；tooltip 含「可能不是最新」                                                                                                      | AC8 二段式        |
| **A5**  | 断网状态点开按钮                               | 面板显示「**离线模式 · 数据截至 X**」+「可能不是最新」；**X 是合理时间**（不是 `null` / `Invalid Date` / `1970`）                                                 | AC8               |
| **A6**  | 清空本地数据后**离线进入**（或首装无镜像）     | 按钮出现**错误色（红）实心圆点**；面板「尚未同步完成…」+「请连接网络后重试」；**不得**出现「数据丢失」字样                                                        | AC9               |
| **A7**  | 造 >200 条任务（超同步上限）后同步             | 按钮**红点**（**在线也成立**）；面板「已加载 N 条」或「任务数量超过同步上限，仅显示部分数据」；**N = 实际行数**（非固定上限、非服务端 total）                     | AC13b + 护栏 B    |
| **A8**  | 观察颜色通道（触发同步中 / 造失败 / 造待推送） | **优先级：同步中（转圈，不额外着色）> 失败（错误色）> 待推送（警告色）> 常态**；**取值与优先级不得变**                                                            | C14 / D2          |
| **A9**  | 角标出现/消失瞬间盯按钮与齿轮                  | **位置/尺寸不变**（不挤动、不换行）；按钮仍 **24×24**                                                                                                             | AC-02.d/e / AC-11 |
| **A10** | 点开面板看底部行                               | 可见「正在加载更多…」（拉取中）与「已加载 N 条…」；**文案与旧顶部条逐字一致**                                                                                     | D-3 / D-6         |
| **A11** | 桌面端看是否有「离线只读」横幅                 | **桌面端不应出现**（桌面写路径不变）；web 端在**尝试写**时才弹一次 toast（≤1200ms 节流）                                                                          | AC10 收口         |
| **A12** | 清掉已读标记后重启                             | 弹**模态确认**、**仅一个「我知道了」按钮**（无取消）；**Esc 关闭 ⇒ 重载后仍会弹**；点「我知道了」⇒ 重载**不再弹**；文案含「明文」、**不含「加密/已加密/受保护」** | AC17              |

## B. 既有回归（SHELL-02，**不得变**）

| #   | 操作步骤                      | 判断点（预期）                                                                                     | 来源   |
| :-- | :---------------------------- | :------------------------------------------------------------------------------------------------- | :----- |
| B1  | 点齿轮展开/收起               | 齿轮可点、两态正常、与按钮**几何无交集**                                                           | AC-01  |
| B2  | 悬停轨道按钮 → 点击           | 出 tooltip「同步」；点击开面板；**零 layout shift**                                                | AC-02  |
| B3  | Tab → Enter → Tab → Esc       | 键盘可开面板、Tab 到「立即同步」、Esc 关闭并**归还焦点**                                           | AC-03  |
| B4  | 触发同步（点「立即同步」）    | 按钮 `disabled` + loading + spin；面板主按钮 loading；首行「同步中…」                              | AC-04a |
| B5  | 造超长错误（断网触发失败）    | 面板内错误**2 行截断**、不撑破；hover 看 `title` 全文                                              | AC-05  |
| B6  | 全新账号（`lastSyncAt` 为空） | 首行显示「**从未同步**」                                                                           | AC-06  |
| B7  | 看轨道底部顺序                | 按钮在**齿轮上方**、**齿轮恒最底**                                                                 | AC-07  |
| B8  | 打开设置对话框                | 同步面板**已收起**、不吞点击                                                                       | AC-09  |
| B9  | 对比按钮与齿轮外观            | **同尺寸同色**；面板默认 `right-center`，贴边可 flip                                               | AC-11  |
| B10 | 用读屏/看 DOM                 | `aria-label` 已本地化；live region **只播摘要**（「同步中…」/「失败 N」），**不含 lastError 全文** | AC-12  |
| B11 | 开设置对话框时按 n/p          | SHELL-01 回归：**n/p 被抑制**；关闭后焦点**归还齿轮**                                              | AC-14  |

## C. 需特别留意（已知缺口 / 观察项）

| #   | 观察点                     | 说明                                                                                                     |
| :-- | :------------------------- | :------------------------------------------------------------------------------------------------------- |
| C1  | **窄屏 ≤445px**            | 窗口拉窄到 ≤445px ⇒ 走**抽屉分支** ⇒ **完全没有同步状态**（已知缺口 `DEF-30`，P2）⇒ **请判断是否可接受** |
| C2  | **内容区底部是否仍被遮住** | 头部零挂载后应已消失；**若仍出现 ⇒ 请回报**（布局根因未定位，`DEF-31`）                                  |
| C3  | **双主题**                 | 暗/亮主题下角标三态（无点/橙点/红点）是否**一眼可分**                                                    |
| C4  | **冷启动观感**             | 无密码进入是否顺滑、有无闪屏/白屏                                                                        |

## D. 机器已覆盖（**无需人眼**）

AC-13 web（独立复验 **33/33**）· i18n 不增不删 · 颜色通道 computed **diff 为空** · 11 项变异**全部如期转红** · 全仓门禁（1386 格式 / 1186 文件 0 error · 1254 例 0 红）—— 这些不必人眼看。

## E. 回报方式

逐项标 ✅ / ❌（+ 截图或一句话描述）；**任何 ❌ 请附「期望 vs 实际」**，我据此定级并派人。