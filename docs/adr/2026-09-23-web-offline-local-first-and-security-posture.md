# 2026-09-23 WEB-OFFLINE：web 本地优先 + 安全姿态选择（两端一致明文）

- **评审对象**：`docs/prds/2026-09-23-web-offline-stage1.md`（TASK-24 阶段一 PRD）；勘察与决策依据 `docs/prds/2026-09-23-web-offline-recon.md`
- **结论**：⚠️ **有条件可行** —— 决策方向成立（约束 **C-46…C-66**）；R1/R2/R4 **已裁决闭合**（§10）· R3 **已裁定 = 接受不可逆**（r3，§10.6）· **AC13a/b/c + AC5b 已补齐（AC 共 24 行）** ⇒ **无剩余阻塞项**
- **范围**：两端数据面姿态（`packages/infrastructure/src/persistence-local/*` + `persistence-sync/*`）+ 启动门链（`apps/desktop/src/renderer/src/AppRoot.vue`、`apps/web/src/views/auth/*`）+ 登出清库接线 + 迁移器 + 护栏（CSP / `userId` 隔离 / 单实例锁）
- **代码边界**：本 ADR 为**纯文档产出**，评审方**未修改任何仓库代码**（只读评审）
- **前序**：`docs/adr/2026-09-10-shell-03-offline-availability.md`（C-01…C-25，离线进入四条件）、`docs/adr/2026-09-13-shell-05-offline-entry-hardening.md`（C-26…C-37）、`docs/adr/2026-09-13-shell-06-offline-backfill.md`（C-38…C-45）、`docs/adr/2026-09-21-sync-nullable-time-tri-state-contract.md`
- **缺陷依据**：`docs/reports/defect-pool.md`（DEF-5/6/7/8/9/10/13/14/15/16/17）
- **实机证据**：`docs/reports/2026-09-23-DEF-PROBE-P1-offline-probes.md`；arch 侧探针报告 P2 / P2b（Z1/Z5/Z7/Z10 + C3/C4 删除影响评估）

---

## 0. 与 SHELL-03 / SHELL-05 / SHELL-06 的关系（修订关系表）

| 关系                  | 说明                                                                                                                                                                                                                                                                                                 |
| :-------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **本 ADR → SHELL-03** | **修订**：删除 C-23 条件④ 与附录 B-2 安全论证③（§2 三分表）；**改写** B-2 安全论证（"门槛 = 本地密码"消失）；**加范围限定**（"业务数据加密存 IndexedDB" → "**desktop 历史姿态**，web 见本 ADR"）。**不改** C-07…C-11（run 边界）、C-22（禁 `navigator.onLine` 鉴权）、C-24/C-25（check-in 终态完备） |
| **本 ADR → SHELL-05** | **修订** C-29（四条件预检 → 判据替换后重写）；`shell-05:344`「数据面 = IndexedDB（加密）本地真源」加范围限定；`shell-05:338/385` 审计结论补 **web 列**（当前 web 无本地数据面 ⇒ 该表结论在 web 不适用）。**不改** C-26…C-37 的导航/注入/打包约束                                                     |
| **本 ADR → SHELL-06** | **不改** C-38…C-45 的推送重试生命周期；但 **C-45「所有推送入口经 `enqueue` 串行」的单写者假设在两端都被削弱**（desktop 未启用单实例锁；web 多标签）⇒ 本 ADR 以 **C-57（desktop 单实例锁）** 与 **C-59（阶段一禁写）** 作为阶段一的**代偿约束**，把跨标签协调显式推给**阶段二**                       |
| **冲突裁决**          | 若本 ADR 与 SHELL-03/05/06 冲突，以**更晚落盘并经评审**者为准，并在两篇「变更管理」互记一行（SHELL-03 与 SHELL-05 需各加一行指向本 ADR）                                                                                                                                                             |

**⚠️ DEF-10 调用点迁移顺序（硬约束，§5.3）**：`checkAndCleanExpired` 从 `unlock-gate.vue:75` 迁往新启动路径，**必须早于**删除解锁门；同一 PR 须「**先加新调用点 + 单测 → 再删旧门**」。违反即**静默回归**（注销到期清理永久不触发）。

---

## 1. 决策（本 ADR 的核心）

| #   | 决策                                                                                                                                | 依据                                                   |
| :-- | :---------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------- |
| D-1 | **两端一致：不加密**（业务数据明文落盘）；`cryptoService` **保留为 passthrough 接缝**（1 文件、可逆、迁移窗口必需），**不整体移除** | A1 档位裁决 + A2-E1；整体移除降级为后续独立决策        |
| D-2 | **仅登出清空本地数据**，且清库 = **按键删除（黑白名单）**，**禁** `localStorage.clear()`；`nao.deviceId` **必留**                   | A1-E1 + P2-Z7（实测：误清 ⇒ 设备漂移 + 会话膨胀）      |
| D-3 | **历史密文在启动门内解包迁移**（搭登录路径静默完成 + 冷启动提示 + **长期保留**入口）                                                | A2-C 节最小设计；用户已拍板                            |
| D-4 | **阶段一 = 只读离线**（远端优先 + 失败回退镜像）；**写路径切换属阶段二**                                                            | PRD §3；A3-§10 豁免 LWW                                |
| D-5 | **接受不可逆**（明文已落盘；**不做密文化回滚路径** —— r3 PM 裁定）                                                                  | PRD §8（已修订）；用户已签                             |
| D-6 | **不做**「进应用永不弹密码框」（读法乙）；**接受「每 7 天需密码一次」**（服务端会话绝对期，**不改服务端会话模型**）                 | P2-Z1 + K8；用户已确认                                 |
| D-7 | **web 数据面接线 = 接 `syncService`（与 desktop 同构）**；被否 ② 自建 pull / ③ 响应级缓存                                           | A4-R1 裁决；PM 已采纳并入 PRD（见 §6 N-5…N-7 / §10.1） |
| D-8 | **账号切换 = 先执行登出清库语义**（复用 `wipeUserData`）；**10041 / 被顶号不清库**                                                  | A4-R4 裁决 + U-2/K4 已签核                             |

---

## 2. 14 项既有条款的「删除 / 修订 / 保留」三分表（**逐条枚举**）

> **口径修正**：recon §5.3 标注为"14 项"，但**逐条枚举实为 15 行**（`shell-05:338/385` 与 `shell-06` C-38·C-40·C-41 各按一条计）。**本 ADR 以逐条枚举的 15 行为准**，避免后续引用歧义。

### 2.1 ❌ 删除（2 项，**必须显式写明**）

| #       | 位置                                                                                 | 原文要点                                                 | 处置                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| :------ | :----------------------------------------------------------------------------------- | :------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **X-1** | `docs/adr/2026-09-10-shell-03-offline-availability.md:404`（附录 B-2 安全论证**③**） | 「**可达范围仅本地已加密数据（其门槛本就是本地密码）**」 | **删除**。⚠️ **不能只删 ③**：同段论证 **① 的门槛 = "凭证存在 + 用户掌握本地保险库密码 + 用户显式选择降级"** 中"本地保险库密码"一并消失 ⇒ **①③ 须同批重写**；**②（服务端从不信任 flag + 10041 三处兜底）与 ④（凭证类失败不得授予，C-23）仍成立，保留**。**重写形式 = 「威胁模型 + 残余风险接受」（§4），不是删除留空**。另注：③「仅本地**已加密**数据」在 **desktop 侧本已部分失真**（结构化字段本就明文）⇒ 属范围限定修订性质                                                   |
| **X-2** | `shell-03:241`（**C-23** 条件④）                                                     | 四条件之一 `cryptoService.isUnlocked === true`           | **删除条件④**。实现层**两处**须同步改：`apps/web/src/views/auth/routes.ts:45`（**真守卫**，desktop 复用同源码）+ `apps/web/src/views/auth/offline-prerequisites.ts:28,37`（`checkOfflineEntryPrerequisites`，**生产零调用点**）。⚠️ **DEF-16 连锁回归**：`isUnlocked` getter = `dek !== null`，`dek` 只由 `setup()/unlock()` 置位 ⇒ 退役门后**恒假** ⇒ 桌面「离线进入」**永久不可达**（silent dead code，**测试全 mock `true` 掩盖**）⇒ **拆除必须补正向单测（DEF-16 / C-62）** |

### 2.2 ✏️ 修订（7 项）

| #   | 位置                                                                                                                                                                                                                                                               | 修订动作                                                                                                      |
| :-- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------ |
| R-1 | `shell-03:54`（§1.2 事实表）                                                                                                                                                                                                                                       | 「业务数据**加密**存 IndexedDB」→ 加范围限定「**desktop 历史姿态**」+ 指向本 ADR                              |
| R-2 | `shell-03:103`（§2 D-1 裁决理由）                                                                                                                                                                                                                                  | 「桌面端本就是『本地**加密**存储 + 增量同步』」→ 补「web 姿态见本 ADR」                                       |
| R-3 | `shell-03:97`（§1.2 本地判据）                                                                                                                                                                                                                                     | 本地判据含 `cryptoService.hasKeyBundle()` ⇒ 随门退役改为「本地镜像存在 + `userId` 匹配」（C-62）              |
| R-4 | `shell-03:399`（附录 B-2 代码块）                                                                                                                                                                                                                                  | 删除 `&& cryptoService.isUnlocked === true // ④ …（= 用户已输入密码）` 行                                     |
| R-5 | `shell-05:344`（§11.3）                                                                                                                                                                                                                                            | 「数据面 = IndexedDB（**加密**）本地**真源**」→ 加范围限定 + 「真源」在 web 的语义待定（§9.4-R1）             |
| R-6 | `shell-05:106`（**C-29** 四条件预检）                                                                                                                                                                                                                              | 判据替换后重写（预检内容改为 C-62 的新判据）                                                                  |
| R-7 | `docs/adr/README.md:27`（SHELL-05 §11 审计结论）+ `docs/adr/2026-09-21-sync-nullable-time-tri-state-contract.md:17`（「桌面端为 local-first」）+ `packages/infrastructure/src/persistence-local/db/local-database.ts:1-8`（头注释「敏感字段以 AES-GCM 密文存储」） | 三处**加 web 列 / 扩两端 / 改注释**；`local-database.ts:264` 的 DB 名 `nao-todo-desktop` **暂不改名**（C-58） |

### 2.3 ✅ 保留（6 项，**明确不改**）

| #   | 位置                                                              | 保留理由                                                                                                                                      |
| :-- | :---------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------- |
| K-1 | `shell-03` **C-15**（昵称缓存白名单）                             | **可反向引用为先例**：本项目**已接受白名单式明文落盘**（`{userId,nickname,cachedAt}` 明文 localStorage）⇒ 新 ADR 应显式引用，否则像"首次破例" |
| K-2 | `shell-03` **C-16**（缓存有效性与清除时机）                       | 与 `userId` 匹配 + 登出清除的语义在明文姿态下**更需保留**                                                                                     |
| K-3 | `shell-05:338` / `shell-05:385`（§11 结论）                       | 结论本身在 desktop 侧成立；**仅需补 web 列**（见 R-5/R-7）                                                                                    |
| K-4 | `shell-06` **C-45**（推送入口经 `enqueue` 串行）                  | 协议层约束不变；**单写者假设**由 C-57/C-59 代偿，跨标签协调推给阶段二                                                                         |
| K-5 | `shell-06` **C-44**（协议冻结 + 记录字段纯追加）                  | 明文/迁移标记若落 `meta` 表（非索引字段）**不违反**；**若需新增 Dexie 索引或 version bump ⇒ 必须回评审**                                      |
| K-6 | `shell-06` **C-38 / C-40 / C-41**（失败分类 / 触发源 / 上限文案） | 与加密无关；阶段一**禁写 ⇒ 这些路径在阶段一不产生流量**，但不得删除                                                                           |

---

## 3. 约束（C-46…C-66）

> 编号接续 SHELL-06 的 C-45；**C-46…C-66 为本 ADR 新增**（未占用任何既有编号；C-64/C-65/C-66 为 r2 新增）。

### 3.1 明文落盘与姿态

- [ ] **C-46 明文落盘（两端同一姿态）**：业务数据（`tasks`/`taskComments`/`taskCheckItems`/`projects`/`tags`/偏好/番茄）**明文**存 IndexedDB；`cryptoService` **保留为 passthrough 接缝**（`encrypt/decrypt` 直通、`isUnlocked` 语义**必须显式定义**——见 C-62）。**禁止**整体移除 `cryptoService`（迁移窗口内必须保留真解密能力）。**禁止**「半明文」（部分字段加密/部分明文）——与"弱化 KEK"同源，制造虚假安全感。
- [ ] **C-47 `meta` 迁移完成标记**：迁移**全库成功后**才写 `meta` 完成标记；标记存在时**不再走迁移路径**（幂等短路）。**禁止**在部分成功时写标记。

### 3.2 迁移红线（四条，**P0**）

- [ ] **C-48 迁移禁走 `repo.update()`**：迁移**必须直写表**（照 `applyPullBatch` 的 `entityToRecord` + `table.put` 写法），**不得**经仓储写路径 ⇒ 否则整库 `markDirty` ⇒ **推送风暴 + 服务端 LWW 被本地伪修改覆盖**。
- [ ] **C-49 迁移期禁同步**：迁移期间**必须**阻止 `syncService` 的 pull/push（否则 pull 把明文覆盖回密文 ⇒ **永不收敛**）。实现方式与解锁顺序一并设计，**不得**依赖"迁移很快"的时序假设。
- [ ] **C-50 幂等可续跑**：迁移必须可在任意中断点续跑（逐表/逐批进度可判定）；**不得**依赖"一次跑完"。中断后重启**不得**出现混合格式读失败。
- [ ] **C-51 双格式双向读取兜底（P0）**：读取侧必须**同时接受** `plain:<原文>` 与历史密文；`cryptoService.decrypt` **对明文输入不得抛错**（现状 `cipher.split(':')` 会抛"密文格式无效" ⇒ **必须修**）。**`key-bundle` 的删除是唯一不可逆开关**：仅在 C-47 标记写入后允许，**且必须长期保留密码入口**（本仓无遥测）。

### 3.3 登出清库

- [ ] **C-52 按键删除黑白名单（P0）**：登出清库**禁止** `localStorage.clear()`，必须按**键白名单/黑名单**逐键 `removeItem`。**白名单（必留）**：`nao.deviceId`（P2-Z7 实测：误清 ⇒ 设备漂移 + 服务端"同设备覆盖旧会话"失效 ⇒ 会话行膨胀）。**建议保留**（设备级偏好）：`THEME_MODE_KEY` / `LANGUAGE_KEY` / `CALENDAR_WEEK_START_KEY`。**黑名单（必清）**：`USER_JWT_LOCALSTORAGE_KEY`、`USER_PROFILE_CACHE_KEY`、`POMODORO_TIMER_SNAPSHOT_KEY` / `POMODORO_FOCUS_SNAPSHOT_KEY` / `POMODORO_SETTINGS_KEY`、`'USER_CONFIRM_UNRESTORE'`、`getStorageKey(tableId)`/`storageKey` 类（键名完备性见探针 **Z13**）。IndexedDB 侧清 11 业务表 + `meta` + `deletionSchedules` + `syncQueue` + `syncCursor`（复用 `deletionService` 既有事务原语）。**⚠️ r2 补强**：必须抽出 **`wipeUserData(userId)`** 作**单一真源**，供 **登出 / 注销到期 / 切换账号** 三处共用（依据：`checkAndCleanExpired` 带前置条件——须存在 `deletionSchedules` 且已到期 ⇒ 登出与切换账号**无法直接调用它**）。清库**必须按 `userId` 过滤**（11 业务表 + `meta` + `syncQueue` + `syncCursor` + `deletionSchedules`）⇒ **禁**整库清、**禁**清他人记录。
- [ ] **C-53 清库可重入（P0）**：清库前置 `meta.pendingWipe = <userId>`（**单事务**内与清库同提交），启动时若存在该标记则**补清**；清库完成后删除标记。⇒ 崩溃/强杀/掉电后重启**必然收敛**（"登出"是唯一清理触发点 ⇒ **登出的可靠性即安全边界**）。
- [ ] **C-54 脏队列阻塞确认（P0）**：登出前判定"是否有未回传写入"**必须直接调 `syncTracker.countDirty(userId)`**；**禁止**读 `syncStatus.pendingCount`（**DEF-13**：仅在 `endRun()` 刷新 ⇒ 滞后）。`>0` ⇒ **阻塞确认**（显示 N + 「先同步」）；离线且 `>0` ⇒ 二次确认并明示不可恢复。**绝不静默清**。

### 3.4 身份与隔离

- [ ] **C-55 `userId` 硬失败（P0，DEF-9）**：`getCurrentUserId() ?? ''` 兜底**必须移除**；`userId` 为 `null`/空时**拒绝一切库操作**（硬失败 + 可见错误），**不得**退化为"空用户"读写。明文姿态下这是**准入条件**（否则跨用户读到明文）。启动时须从 JWT 重建 `localSession`（web 现无重建点）。
- [ ] **C-62 离线进入判据替换（P0，DEF-16 / K9）**：删除 C-23 条件④；新判据 = **`offlineEntryGranted && JWT 可解析 && localSession.getCurrentUserId() === jwtUserId && 本地镜像存在`**（**两处实现同步改**：`routes.ts` 真守卫 + `offline-prerequisites.ts`）。**必须补正向单测**（现有测试只覆盖 `isUnlocked === false` 的**拒绝**路径 ⇒ 删除后无守护）。若改走"passthrough 仍置位 `isUnlocked`"的备选方案 ⇒ **必须在本 ADR 变更记录中显式声明**，否则离线进入静默失效。

### 3.5 启动门与调用点

- [ ] **C-56 启动门内迁移（P0）**：迁移**必须**在主界面挂载前、**启动门内**完成（冷启动进入"感觉即时"）；**批量事务**（5k 条 0.3–1.5s 量级），**禁止逐条事务**（数秒~十几秒）。
- [ ] **C-61 门退役顺序硬约束（P0，DEF-10）**：`checkAndCleanExpired` **必须**迁往**单一收敛点** `bootstrapLocalData(userId?)`（`userId ??= resolveUserIdFromStoredJwt()` → `localSession.setCurrentUserId(userId)` → `await deletionService.checkAndCleanExpired(userId)`），且**必须早于 `syncService.start()`**（`start()` 读 `deletionSchedules` 决定跳过启动同步）。调用点覆盖：**① desktop 冷启动（AppRoot，替代 `unlock-gate.vue:75`）② web 阶段一门 B 通过后的等价收敛点（必须）③ 常驻跨 7 天（挂既有 `registerBackfillTriggers` 前台恢复/`online` 触发源）**。**顺序：先加新调用点 + 单测 → 再删旧门**；新调用点**必须自带单测**（现有守护随门消失）。可验收判据 = 一条**顺序断言**（探针 **Z14** 预期无此断言，需补）。

### 3.6 护栏

- [ ] **C-57 desktop 单实例锁（P0）**：启用 `requestSingleInstanceLock`（实测**当前未启用**）。理由：`(user_id, device_id)` 会话 upsert、pull 游标 RMW、`syncQueue` 单写者、迁移竞态**都假设单写者**。
- [ ] **C-58 CSP 必须（P0，DEF-8）**：两端**均须有生效 CSP**。**desktop**：生产走 `win.loadFile`（`file://`，**无 HTTP 响应头**）⇒ CSP **只能**落 `<meta http-equiv="Content-Security-Policy">`。**web**：SPA **不由 `nao-todo-server` 托管**（服务端无 Static/NoRoute）⇒ CSP 必须落**静态托管侧**（**不在本仓可验收**，见 §9.3-AC12）。明文姿态下 CSP 从"建议"升为**准入条件**。
- [ ] **C-58b DB 改名暂缓**：保留 `nao-todo-desktop`（`local-database.ts:264`）。**改名 = 新库 ⇒ 存量数据被孤立**；**迁移完成前不得改名**。
- [ ] **C-59 阶段一不得产生 `markDirty`（P0）**：阶段一为**只读**；**任何**写路径都不得入队。**desktop 现状能写**（本地仓储 + `markDirty`）⇒ 阶段一**必须在 UI 层统一禁写**（**不要逐个改仓储**，会漏），并以 **P1 探针 §3.3 的 24 项清单为唯一真源**。若产生脏队列 ⇒ **C-1（登出丢未同步写入）与 LWW 立即变必需**（阶段一豁免失效）。
- [ ] **C-60 数据新鲜度文案三分（P0）**：① 在线·远端数据 ⇒「**已更新**」；② 回退镜像 ⇒「**离线模式 · 数据截至 {时间}**」+「可能不是最新」；③ `mirrorPulledAt === null` 或空镜像 ⇒「**尚未同步完成，数据可能不完整**」（**禁止**显示"截至 X"）。**web 必须新引入 `mirrorPulledAt`**（**探针 Z12**：web 无 `syncService` 接线 ⇒ 复用 `syncStatus.lastSyncAt` 恒 `null`；且 `lastSyncAt` 语义 = "最后一次**完全成功**同步"（`endRun` 仅 `lastError === null` 时推进），**不等价"远端最新"**）。**空库 vs 未同步完成必须可区分**。**⚠️ r2 补强——截断不得谎报**：若因护栏 A 的上界未拉完 ⇒ **`mirrorPulledAt` 不得推进**（文案落 ③「尚未同步完成，数据可能不完整」）+ **触顶提示**；**禁止**在未完整拉取时显示「数据截至 X」（C-60 三分为**互斥穷尽**）。

### 3.7 其它

- [ ] **C-63 不可逆的上线纪律**：姿态不可逆 ⇒ **DEF-5 / DEF-6 必须先合入并验证**，再切姿态；**阶段一单独发版**；上线前完成 §4.5 的残余风险接受记录。
- [ ] **C-64 账号切换 = 先清库（P0，r2 新增）**：**切换账号必须先执行登出清库语义**（复用 `wipeUserData(userId)`，见 C-52）——理由：① 明文下「用户 A 数据留在设备」即 **RS-1（共享设备）实质暴露**（`userId` 隔离只防串读，不防同设备他人可见）；② 保持**清理触发点单一**（C-53 的 `pendingWipe` 可重入机制不必复制）；③ 与 **U-2/K4（10041 不清库）不冲突**（切换账号是用户主动动作，非被顶号）。**禁**整库清、**禁**清他人 `deletionSchedules`。
- [ ] **C-65 `hooks/usecases/**` 共享化（P0，r2 新增）**：web 接线（C-66）之前**必须**先把 `apps/desktop/src/renderer/src/hooks/usecases/**`（**13 个文件**）共享化为**单一真源**（提取到共享包或等价机制）；该步为**纯搬迁、零行为变更、可独立验证**（`vp check` + 既有测试全绿 + 桌面回归）。**禁**在 web 侧复制第二份同构文件（违反 D-1「两端一致」且制造双真源）。
- [ ] **C-66 web 数据面接线 = 接 `syncService`（P0，r2 新增，D-7）**：web 采用与 desktop **同一份** `hooks/usecases/**`（本地仓储接线）+ 接入 `syncService`/`localSession`/`deletionService`/`cryptoService`（passthrough）。**连带前置**：**C-59 必须覆盖 web**（web 接本地仓储后写路径会 `markDirty` 入队；不禁写则阶段一 LWW 豁免失效且 C-1 立即可达）；**C-61 调用点②**（web 的 `bootstrapLocalData` 等价物，保证 `checkAndCleanExpired` 早于 `syncService.start()`）；**pull 单主**（`navigator.locks`）。

---

## 4. 威胁模型 + 残余风险接受

### 4.1 安全边界搬迁（一句话）

**从「设备上的本地密码」搬到「浏览器/系统账号 + 设备持有本身」= 「设备即凭据」。**

### 4.2 暴露窗口（量化）

| 端          | 窗口 = 从 → 到                        | 实际长度                                                                                                                                                                                                         |
| :---------- | :------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **desktop** | 登录（建库/落明文）→ **用户主动登出** | **无上界**：应用常驻、**当前未启用单实例锁**；JWT 失效（10041）时 `clearAuthData()` **只清 localStorage、不清 Dexie** ⇒ **明文在 JWT 失效后仍留磁盘**，直到用户登出 ⇒ 以 **C-53（可重入清库）** 作为唯一收敛保证 |
| **web**     | 同上 + 浏览器驱逐                     | 有**随机上界**（**不申请 `persist()`**，作为事实上的过期机制）                                                                                                                                                   |

**关键推论**：**明文数据的存在时间 > JWT 有效期（48h）** ⇒ **真正的边界是「登出」这个动作的可靠性**（崩溃/强杀/掉电未执行完 = 明文长期残留）。⇒ C-53 为**承重约束**。

### 4.3 「无密码」的硬上限（不可绕过）

| 闸                  | 值                                | 说明                                                                          |
| :------------------ | :-------------------------------- | :---------------------------------------------------------------------------- |
| 单次 JWT            | **48h**                           | `ExpOffset` 全仓无赋值点 ⇒ 默认值；**客户端完全不校验 `exp`**，仅响应 10041   |
| 会话（`ExpiredAt`） | **7 天，绝对，不滚动**            | `sessionRepoImpl.go:47` 仅创建时写入；`UpdateToken` 换发**不动** `expired_at` |
| 触发点              | **仅冷启动**（`check-in.vue:37`） | 无周期性 ⇒ **常驻 >48h 必被踢回登录页**（DEF-15）                             |

⇒ **「不弹密码框」的现状硬上限 = 连续 7 天**（前提：每 48h 内至少一次联网冷启动）；**7 天后连 checkin 都失败 ⇒ 只能 `signIn`（需密码）**。⇒ **读法乙（永不弹密码框）在现状服务端语义下不可能达成**；要达成必须改服务端会话模型（滚动续期 / refresh token / 长期会话）⇒ **D-6 明确不做**（K8 已确认）。

### 4.4 明确接受的残余风险（**逐项显式接受，不是遗漏**）

| #    | 残余风险                                                             | 实测/依据                                                                                                                                                                                         | 接受理由与代偿                                                                            |
| :--- | :------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------------------------------- |
| RS-1 | **共享设备**：任何能打开该设备/浏览器 profile 的人可直接读到全量明文 | A1 威胁 delta：**web 侧已明文存 bearer JWT 且全仓零 CSP ⇒ 恶意扩展/同源页面增量≈0**；明文**明确增量仅"共享设备"与"物理取盘"**                                                                     | 用户已拍板（无密码优先）；代偿 = 登出清库（C-52/C-53）+ 文案提示                          |
| RS-2 | **物理取盘**：磁盘上无保护                                           | 同上                                                                                                                                                                                              | 同上；**若后续需要，可加"可选锁定"（A1 档位④）作为增强，不改默认姿态**                    |
| RS-3 | **`file://` 跨目录共享（实测确证）**                                 | Electron 43 实测：同目录与**不同目录**页面均读到 localStorage 与 IndexedDB；`location.origin === "file://"` ⇒ **同 profile 下任何本地 HTML 都能读本应用数据**                                     | **机密性边界在 desktop 侧失效** ⇒ 以 C-58（`<meta>` CSP）+ 限缩 renderer 加载面代偿       |
| RS-4 | **web 驱逐（不申请 `persist()`）**                                   | desktop 实测 `navigator.storage.persisted() === true`（**不受驱逐**，修正早期推断）；web Chromium best-effort；**Safari/WebKit ITP：7 天无交互即删全部脚本可写存储** ⇒ **web 驱逐风险 100% 暴露** | 作为**事实上的过期机制**被接受（与"离线能看"存在张力 ⇒ 由 C-60 的"尚未同步完成"文案兜底） |
| RS-5 | **登出后离线 = 死路**                                                | 登出即废除 JWT（与"JWT 长期有效"语义互斥）                                                                                                                                                        | 接受 + **文案明示**（「登出将清除本地数据，需联网重新登录」）                             |
| RS-6 | **被顶号 / 10041 离线不可知**                                        | 联网后第一请求即清认证；用户会感到"离线改的突然全没了"                                                                                                                                            | 接受（**K4 未决**：10041 是否视同登出并清本地数据 ⇒ **待用户拍板**，PM 倾向**不清**）     |

### 4.5 上线前必须完成的残余风险记录

- **接受不可逆**（D-5 已签；r3 PM 裁定：**不做密文化回滚路径**、**不承诺回滚**）⇒ 上线前做的是**前置验证 + 灰度**（DEF-5/6 先合入验证 → 再切姿态；阶段一单独发版）。**原「上线前必须做一次回滚演练」要求已删除。**
- RS-3 / RS-4 的**用户可见声明**（设置页 + 首次迁移提示）。

---

## 5. 迁移设计（最小充分集）

### 5.1 最小充分集（不丢存量数据）

① `meta` 的 `key-bundle` 记录**不删不覆盖** ② crypto **真解密实现保留** ③ 由 `hasKeyBundle` 驱动的**密码入口保留** ④ **迁移完成标记**（C-47）。
⚠️ **真数据丢失风险（必须写进实现说明）**：升级后**迁移完成前从未输入过旧密码** ⇒ 密文**永久不可解**（DEK 无第二把钥匙）⇒ ③ 必须**长期保留**（本仓无遥测，无法确认"全部用户已迁移"）。

### 5.2 迁移器写法（正确性）

- **直写表**（`entityToRecord` + `table.put`），**不 `markDirty`**（C-48）。
- **幂等可续跑** + **双格式双向读取**（C-50/C-51）。
- **禁用**逐条事务（C-56）。

### 5.3 门退役与调用点迁移（顺序硬约束）

| 步骤 | 动作                                                                                | 顺序要求                         |
| :--- | :---------------------------------------------------------------------------------- | :------------------------------- |
| 1    | 加 `bootstrapLocalData()`（含 `checkAndCleanExpired`）+ **单测**                    | **必须先于步骤 3**               |
| 2    | 接入 `AppRoot`（desktop）+ web 阶段一收敛点；**保证早于 `InitialSyncGate.start()`** | 同上（**C-61**）                 |
| 3    | 删除 `unlock-gate.vue`（解锁门退役）                                                | **最后**；违反即 DEF-10 静默回归 |
| 4    | 替换离线进入判据（C-62）+ 补**正向**单测（DEF-16）                                  | 与步骤 3 同批                    |
| 5    | 删 C-23 条件④ / 改 B-2 论证（§2.1）                                                 | 与步骤 3 同批                    |

---

## 6. 被否方案（**否决理由留档**）

| #   | 方案                                                    | 否决理由                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| :-- | :------------------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| N-1 | **弱化 KEK**（降低 KDF 强度/固定盐等）                  | **与明文安全等价，但制造虚假安全感**（攻击者拿到的仍是可解密的密文 + 弱化后的可暴力破解 KEK）⇒ 比明文更差                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| N-2 | **半明文字段**（只落部分字段明文）                      | 与 N-1 **同源**：暴露面未实质收窄，但复杂度与"哪字段加密"的长期维护成本上升 ⇒ 列为**后续可选项（D1b）**，不作为阶段一方案                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| N-3 | **web 仅把 IndexedDB 当"候选集读缓存"**                 | ① 双数据源（远端 + 缓存）语义分裂 ② **无失效钩子**（`subscriber` 5 事件中 web 不发 `nao-todo:data-changed`）③ 收益不足以抵复杂度（A3-E 已裁决）                                                                                                                                                                                                                                                                                                                                                                                                                           |
| N-4 | **直接删密码不迁移**（不做解包）                        | **存量密文永久不可恢复**（DEK 无第二把钥匙）⇒ 违背"迁移不可丢数据"（PRD §6 可靠性）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| N-5 | **web 自建 pull 到镜像**（不引入同步引擎，A4-R1 选项②） | ① **无法复用引擎原语**：`applyPullBatch` 为 **private**，`SYNC_TABLES`/`PULL_BACKTRACK_MS`/`rewindIsoCursor` **均模块私有**（`sync-service.ts:110/250/273`）⇒ 只能 **(2a)** 重构 `sync-service.ts`（**改动 desktop 正在运行的引擎，风险外溢到已上线端**）或 **(2b)** web 重写（**重新引入 DEF-SYNC-05 类游标 bug**：回拉窗口 Δ / `cursorId` 归零 / 时区形态 / 禁字符串 max）；② `deletionSchedules` 跳过启动同步**不继承** ⇒ 宽限期内 web 照常同步 = **端差异**（正是 D-1 要消除的）；③ **阶段二必然要 `syncService` ⇒ 本方案大部分作废 ⇒ 净成本更高**（+4–6 人天且返工） |
| N-6 | **web 响应级缓存**（不建镜像，A4-R1 选项③）             | **产品目标不成立**：只覆盖"已浏览页"，**不满足「离线能看全部」**；且与 N-3 同源（双数据源 + 无失效钩子）                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| N-7 | **`id desc` 起拉**（改为按 id 倒序拉取）                | ① 在"续拉至无更多"下**不需要**（循环终会覆盖最新）；② 其唯一价值（截断时先拿最新）**需改服务端 keyset 方向** ⇒ 触碰 **C-44（协议冻结）** ⇒ **必须回评审**；本单不做，如确需另立项                                                                                                                                                                                                                                                                                                                                                                                         |

---

## 7. 未决 / 待拍板（**不得带缺陷开工**）

| #       | 事项                                                                         | 状态                                                                                                                                                      |
| :------ | :--------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **U-1** | **D1b 字段范围**（全字段明文 vs 精简：不落 `description`/评论正文/番茄备注） | **未决**（recon §6.3；影响 §4 暴露面与 AC8/AC9 的"离线能看什么"）→ **✅ 已签核（2026-09-23 用户）：全字段明文 + 首次进入明确告知**（否掉「精简」）        |
| **U-2** | **K4：10041 / 过期是否视同登出并清本地数据**                                 | **未决**（PM 倾向**不清**，需用户确认）⇒ 影响 AC5/AC7 口径 → **✅ 已签核（2026-09-23 用户）：10041 / 被顶号**不清**本地数据**（见 D-8）                   |
| **U-3** | **web 数据面接线方式**（见 §9.4-R1）                                         | **未决**（**放行前必须澄清**）→ **✅ 已裁决 = ① 接 `syncService`（与 desktop 同构）**（A4-R1；PM 已采纳并入 PRD；见 D-7 / C-65 / C-66 / §10.1）           |
| **U-4** | **回滚是否要求可执行**（见 §9.4-R3）                                         | **✅ 已裁定（2026-09-23 PM）：不要求可执行回滚** —— 接受不可逆；密文化迁移器**不纳入范围**（不加人天）；删除"上线前回滚演练"要求，改为**前置验证 + 灰度** |
| U-5     | 探针 Z12–Z19                                                                 | 上线前必过                                                                                                                                                |

---

### 7.1 新增未确认项（r2 登记，见 §10.4）

| #       | 探针                                                                                                                                                                                                                                     | 预期/影响                                                                     |
| :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------- |
| **Z17** | 本地 `users`/`userConfigs` 表的**写入点**（两端用户域均走远端 `getRequesterImpl()` + `newUserRepository(requester)` ⇒ 本地表疑未使用）                                                                                                   | 判断本地两表是否死代码 ⇒ 影响 C-52 清库范围与"离线用户域"口径                 |
| **Z18** | **偏好表落库来源**：`applyPullBatch`（`sync-service.ts:509-560`）**只写 7 张 `SYNC_TABLES`**，**不写** `projectPreferences`/`tagPreferences`；而 desktop 的 `use-project-usecase.ts:14` / `use-tag-usecase.ts:11` **确实接本地偏好仓储** | **镜像可能不含偏好** ⇒ 影响「离线能看」覆盖度（**两端一致，非 R1 选项差异**） |
| **Z19** | **续拉上界的真实成本**（墓碑密集账号单次启动的轮数/耗时）                                                                                                                                                                                | 用于**校准护栏 A 的 N**（建议初值：每表 ≤10 轮或总预算 3s）                   |

---

## 8. 变更管理

- 本 ADR 的实现偏离**必须**回架构评审；新增浮层/例外须在本篇留条目。
- **归档索引**：PM 归档时需在 `docs/adr/README.md` 索引表新增一行（日期 2026-09-23 / 主题「WEB-OFFLINE web 本地优先 + 安全姿态（两端一致明文）」/ 决策要点 / 状态 ⏳ 有条件通过），并在**篇间关系**表补 **本 ADR ↔ SHELL-03 / SHELL-05 / SHELL-06** 三行；**待拍板事项跟踪**表补 U-1…U-4。
- **互记要求**：SHELL-03 与 SHELL-05 的「变更管理」各加一行指向本 ADR（**删除 X-1/X-2 必须被记录**）。

---

## 9. PRD 架构评审意见（`docs/prds/2026-09-23-web-offline-stage1.md`）

### 9.1 约束完备性 —— ✅ 基本完备（2 处补强）

| 项          | 评价                                                                                                                                                |
| :---------- | :-------------------------------------------------------------------------------------------------------------------------------------------------- |
| 不变量 5 条 | ✅ 与 C-46/C-52/C-47/C-59/C-55 一一对应                                                                                                             |
| 边界 4 条   | ✅ 但"web 驱逐风险 100% 暴露"是**风险接受**而非边界 ⇒ 建议移到风险节（已由本 ADR §4.4-RS-4 承接）                                                   |
| 迁移红线    | ✅ 四条齐（C-48/C-49/C-50/C-51）；**建议补第 5 条**：`key-bundle` 删除时机（C-51 已补）                                                             |
| 清库        | ✅ C-52/C-53/C-54；**建议补**：清库的 **IndexedDB 侧范围**（11 表 + `meta` + `schedules` + `queue` + `cursor`）与"切换账号 = 先登出语义"（§9.4-R4） |
| 护栏        | ✅ CSP / `userId` / 单实例锁；**建议补** `pendingWipe` 的**故障注入测试**要求（否则 AC5 无法证"补清"）                                              |
| 缺          | ⚠️ **i18n 键要求**（新增文案：离线标识 / 待升级 / 清库确认 ⇒ 三处 `locales` 键，沿用 shell-06 C-41 先例）                                           |

### 9.2 NFR 转译 —— ⚠️ 大体到位（2 处需标注）

- ✅ 可靠性/一致性/安全/兼容/可观测转译清晰。
- ⚠️ **性能**："批量事务 5k 条 **0.3–1.5s**" 是 **A2 的推断区间**（非实测）⇒ 应标注"**待实测校准**"，并补"**主界面未挂载前**"的可观测断言（否则 AC 不可测）。
- ⚠️ **性能分端**：若 web 不接迁移（见 §9.4-R1）⇒ 该指标**仅适用 desktop**，PRD 未分端。
- ⚠️ **可观测**：NFR 提"结构化日志"但**无对应 AC** ⇒ 建议补一条（迁移/清库耗时 + 结果，禁 PII）。

### 9.3 AC1–AC17 可测性评审 —— ⚠️ **6 处需修订（PM 已全部修订：AC1b/AC2/AC3/AC6/AC12/AC16a-b + AC10 引用真源；AC13 已拆 a/b/c、新增 AC5b；AC 共 24 行）**

| AC       | 可测性 | 问题与建议                                                                                                                                                                                                                                           |
| :------- | :----- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC1      | ⚠️     | Given「有镜像 + **无 `key-bundle`**」**未覆盖真实主路径**（存量用户迁移完成后**仍有 `key-bundle`**，除非删除）⇒ **建议补 AC1b**：有 `key-bundle` + **迁移完成标记** ⇒ 冷启动仍无密码。另："不出现任何密码输入"建议落为 DOM 断言（无 password input） |
| AC2      | ⚠️     | 「登录路径已带密码 ⇒ 静默迁移」**仅对 desktop 成立**（web 现无 `persistence-local` 接线，`use-auth-usecase.ts:41 ensureUnlocked` 在 web 是否执行取决于 §9.4-R1）⇒ **必须标注适用范围**，否则 AC 不可测                                               |
| AC3      | ⚠️     | 「不出现混合格式读失败」需**显式断言**"明文行可读"（现状 `decrypt` 对明文**抛错**）⇒ 建议拆为两条：① 续跑完成 ② 混合格式下读取成功（含 `plain:` 与历史密文各 1 例）                                                                                  |
| AC4      | ✅     | 可测（标"待升级" + `key-bundle` 未删）                                                                                                                                                                                                               |
| AC5      | ⚠️     | 需**故障注入**才能证"崩溃补清"（否则只测了正常路径）⇒ 建议明确注入点（清库事务前中断）                                                                                                                                                               |
| **AC6**  | ❌     | **阶段一不产生脏队列 ⇒ Given `countDirty > 0` 在阶段一不可达**（除非**存量升级用户遗留历史脏队列**）⇒ **必须写明前提**，否则该 AC 无法执行 ⇒ 建议改为"**存量升级用户**（有历史脏队列）"并标注阶段二复验                                              |
| AC7      | ✅     | 可测（网络类 vs 凭证类分支）                                                                                                                                                                                                                         |
| AC8      | ✅     | 可测（三分文案）；建议加"**不得出现 `null`/1970 字样**"的负向断言                                                                                                                                                                                    |
| AC9      | ✅     | 可测                                                                                                                                                                                                                                                 |
| AC10     | ⚠️     | "24 项"需**引用唯一真源**（`docs/reports/2026-09-23-DEF-PROBE-P1-offline-probes.md` §3.3）⇒ 建议 AC 内直接引用清单路径；并断言 `markDirty` **零调用**                                                                                                |
| AC11     | ✅     | 可测（null/空 ⇒ 硬失败 + 跨账号不串）                                                                                                                                                                                                                |
| **AC12** | ❌     | **web CSP 不在本仓可验收**（SPA 由静态托管侧提供 CSP，本仓无托管代码）⇒ **必须改为**：desktop `<meta>` 在本仓断言 + **web CSP 列入部署侧上线检查单（人工核验）**，否则该 AC 永远无法闭环                                                             |
| AC13     | ✅     | 可测（>200 行含最新）；建议补**时间上界**（如"启动 1 次内 ≤N 轮/≤X 秒"）以便判定                                                                                                                                                                     |
| AC14     | ✅     | 可测（顺序断言）                                                                                                                                                                                                                                     |
| AC15     | ✅     | 可测（**正向**可达）                                                                                                                                                                                                                                 |
| **AC16** | ❌     | **混装两件事**：desktop 单实例锁 / web 多标签"无残留明文"。后者在**阶段一（只读）不可复现**（A3 §9-⑤ 与阶段二绑定）⇒ **建议拆为 AC16a（desktop 单实例锁生效）+ AC16b（web 多标签：仅断言登出广播后各标签 store 清空 + 跳登录页）**                   |
| AC17     | ✅     | 人眼验收，合理                                                                                                                                                                                                                                       |

### 9.4 遗漏风险（4 项；**R1/R2/R4 已裁决、R3 已裁定** —— 见 §10）

| #      | 遗漏                                                                                                                                                                                                                                                                     | 影响                                                                                                                                                                                                        |
| :----- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R1** | **web 数据面接线方式未定义**：PRD 要求"两端一致明文 + 只读离线镜像"，但 **web 从未接线 `persistence-local`/`syncService`**（A3 实测：web 0 引用；web/desktop 是**同一份源码的两个构建**，仅 `@/hooks` 别名不同）⇒ **web 是"接 `syncService`"还是"自建 pull 到镜像"未定** | 决定 DEF-6（C5）、单写者、多标签、`syncCursor`/`deletionSchedules` 跳过逻辑**是否全部变成 web 前置**；也决定 AC2 的适用范围与人天。**最大范围缺口**                                                         |
| **R2** | **DEF-6（C5）的修复口径未绑定 AC13 的判定**                                                                                                                                                                                                                              | AC13 只说"含最新任务"；未规定"≤200 行/表/次启动、最旧优先、无续拉"的**新语义**（续拉？改 `id desc`？）⇒ 实现自由度过大                                                                                      |
| **R3** | **回滚路径实际不存在**（原描述："回滚需再迁移一次（密文化）"但密文化迁移器不在范围）                                                                                                                                                                                     | **✅ 已裁定（2026-09-23 PM）：接受不可逆** —— **不做密文化回滚路径**（不纳入范围、不加人天）；**删除"上线前必须做一次回滚演练"**，改为**前置验证 + 灰度**；PRD §8 已修订为"不可逆 = 已接受事实"（见 §10.6） |
| **R4** | **多账号切换（同设备不同用户）未覆盖**                                                                                                                                                                                                                                   | AC5 只覆盖"登出"；明文下"切换账号"若不清库 ⇒ **跨用户明文残留**（与 C-55 同风险等级）⇒ 建议明确"切换账号 = 先执行登出清库语义"                                                                              |

### 9.5 放行结论

⚠️ **有条件可行**：

1. **R1 / R2 / R4 已裁决**（R1 = ① 接 `syncService`，见 §10.1；R2 = 最小口径 + 护栏 A/B，见 §10.2；R4 = 切换账号先清库，见 §10.3）⇒ **放行前的必答项已闭合**；
2. **修订 6 处 AC**（AC1b / AC2 适用范围 / AC3 拆分 / AC6 前提 / AC12 外部依赖 / AC16 拆分）+ AC10 引用清单真源 —— **✅ 已闭环**（PM 已改 AC2/AC12/AC16a-b/AC18；**AC13a/b/c + AC5b 已补齐，AC 共 24 行**，见 §10.5）；
3. **NFR 两处标注**（性能待实测 + 分端；可观测补 AC —— PM 已加 AC18）；
4. **硬前置不变**：DEF-5、DEF-6、DEF-10、DEF-16、DEF-8/DEF-9；**r2 新增前置**：**C-65（hooks 共享化，step 0）**、**C-59 覆盖 web**、**C-61 调用点② + pull 单主**；
5. **U-1 / U-2 已签核**（全字段明文 + 首次进入告知；10041 不清库）⇒ 口径已稳；
6. **R3 已裁定**（r3：**接受不可逆**，见 §9.4-R3 / §10.6）⇒ **无剩余阻塞项**。

**架构技术签字**：**C-46…C-66 为技术可行**；R1/R2/R4 裁决与 **R3 裁定（接受不可逆）**均已落盘，**AC13a/b/c + AC5b 已补齐（AC 共 24 行）** ⇒ **无剩余阻塞项，可放行实现**。

---

## 10. r2 增补：R1 / R2 / R4 裁决与落点

### 10.1 R1 裁决：web 数据面接线 = 接 `syncService`（同构，D-7 / C-66）

**裁决**：采用 **① 接 `syncService`（与 desktop 同构）**；② 自建 pull / ③ 响应级缓存**被否**（理由见 §6 N-5/N-6/N-7）。

**事实基线（读源码）**：

| 事实                                                                                                                                                   | 证据   |
| :----------------------------------------------------------------------------------------------------------------------------------------------------- | :----- |
| 两端**同一份源码**：`apps/desktop/electron.vite.config.ts:26-33` alias `@/hooks → desktop hooks`、`@ → apps/web/src`；web 仅 `@ → ./src`               | 读源码 |
| **端差异 = 每个 usecase 文件一行**（`newLocalTaskRepository()` vs `new TaskRepoImpl(getRequesterImpl())`）；需切换 = `hooks/usecases/**` **13 个文件** | 读源码 |
| 本地数据面现成：`persistence-local/**`（Dexie 11 表 + `newLocal*Repository`）+ `persistence-sync/**`（`syncService`/`syncTracker`/`deletionService`）  | 读源码 |

**影响面裁决**：

| 影响面                           | 结论                                                                                                                                            |
| :------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------- |
| DEF-6 / C5                       | **变 web 前置**，但**修一次两端同受益**                                                                                                         |
| 单写者（C-45）                   | 变前置；**阶段一可降级为软前置**（只读 ⇒ 无 push；多标签 pull 游标 RMW 后果 = 重复拉取（幂等）而非丢数据）⇒ 建议 `navigator.locks` 给 pull 单主 |
| 多标签（登出复活 N1）            | **阶段一不可复现**（只读 ⇒ 标签 B 无法写回），与阶段二绑定                                                                                      |
| `syncCursor`                     | **需要**（引擎自带，含 DEF-SYNC-05 修复）                                                                                                       |
| `deletionSchedules` 跳过启动同步 | **自动继承 ⇒ 端语义一致**；但**必须补 C-61 调用点②**                                                                                            |
| AC2 适用范围                     | **desktop only**（web 是新 origin/新库、**无历史密文 ⇒ 无迁移面**）；web 仅需"首次进入明文告知"                                                 |
| 人天                             | 阶段一 **+3–5**（hooks 共享化 1–1.5 + web 门/离线回退读 1–2 + 禁写开关覆盖 web 1–1.5）⇒ **19–30 → 22–35**（推断）                               |

**① 的三条硬前置**：**P1 = C-65**（hooks 共享化，step 0，**排在 web 接线之前**）· **P2 = C-59 覆盖 web** · **P3 = C-61 调用点② + pull 单主**。

### 10.2 R2 口径：DEF-6 修复（与 AC13 绑定）

- **最小口径 = 「续拉至无更多」**，且**不改墓碑 / 窗口语义**（保持 `.Unscoped()`、`updated_at ASC`、keyset `updated_at > c OR (=c AND id > cursorId)`、`limit = PULL_LIMIT(200)`）。
- **判据（易踩点）**：服务端 `SyncPullTableRes.Total = int64(len(items))`（`interfaces/controllers/sync.go:212`）**不是剩余总数** ⇒ **只能**用 **`items.length < limit`** 判定"无更多"；**禁止**用 `Total`。
- **游标**：续拉循环**必须复用同一** `rewindIsoCursor(cursorAt, PULL_BACKTRACK_MS)`，回拉时 `cursorId` 归零，**每轮游标只在成功时推进**（保持"只前进不后退"）。
- **护栏 A（必须）**：**每表单次启动最多 10 轮（=2000 行）或总预算 3s**，取先到者（墓碑计入窗口 ⇒ dev 库实测 436 行中墓碑 247 ⇒ 轮数会放大；N 由 **Z19** 校准）。
- **护栏 B（必须）**：截断时 **`mirrorPulledAt` 不得推进** + 触顶提示（见 C-60 r2 补强）。
- **`id desc` 起拉：不做**（见 §6 N-7）。
- **测试要求**：除既有 `def6-mirror-completeness.test.ts`（250 行、无墓碑、断言落满 + 含最新）外，**必须补**：① 墓碑密集（如 250 活 + 250 墓碑）仍能拉完；② 上界截断时 `mirrorPulledAt` 不推进 + 触顶提示。

### 10.3 R4 裁决：账号切换 = 先清库（D-8 / C-64）

- **裁决**：**是**——切换账号**必须先执行登出清库语义**（复用 `wipeUserData(userId)`）。
- **落地缺口**：**不存在"按需清库"原语**（唯一实现 `checkAndCleanExpired` 带"须有 `deletionSchedules` 且已到期"前置条件）⇒ **必须抽 `wipeUserData(userId)`** 作单一真源（C-52 r2 补强）。
- **可行性前提（已核）**：清库**全部按 `userId` 过滤**（11 业务表 + `meta` + `syncQueue` + `syncCursor` + `deletionSchedules`）⇒ **多账号同设备不会误清他人数据** ✅。
- **一处不一致（低风险）**：同方法末尾 `clearCachedNickname()` 是**全局清**（不按 `userId`）⇒ 会清掉另一账号的昵称缓存（C-16 按 `userId` 校验，影响小）。

### 10.4 新增未确认项

见 **§7.1**（Z17 / Z18 / Z19）。

### 10.5 AC 补齐确认（**已闭环**，2026-09-23 `f20cf83e`）

| #    | 建议                                                                                                                                                           | 状态                                |
| :--- | :------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------- |
| AC13 | 拆 **AC13a**（无墓碑压力：拉满且含最新）/ **AC13b**（墓碑密集：上界内拉完 **或** 不推进 `mirrorPulledAt` + 触顶，**二者必居其一**）/ **AC13c**（不再停在 200） | **✅ 已补齐**（AC13a/b/c 三行在位） |
| AC5b | **账号切换清库**：A 的 11 表零残留 + A 的 `deletionSchedules` 不被误删 + B 从远端重建                                                                          | **✅ 已补齐**（AC5b 在位）          |

---

### 10.6 R3 裁定（r3）：**接受不可逆**

- **裁定（2026-09-23 PM）**：**不做密文化回滚路径**（密文化迁移器**不纳入范围**、**不加人天**）；**删除**「上线前必须做一次回滚演练」要求；上线前做的是**前置验证 + 灰度**（DEF-5/6 先合入验证 → 再切姿态；阶段一单独发版）。
- **PRD §8 已修订为**：`回滚 | ⚠️ 不可逆 = 已接受事实（D-5 已签）：明文已落盘，不做密文化回滚路径 ⇒ 上线前做的是前置验证与灰度，不承诺回滚`。
- **ADR 同步**：D-5 措辞、§4.5（删除回滚演练要求）、§9.4-R3（改为已裁定）、U-4（改为已裁定）、§9.5（移除 R3 阻塞项）、头部结论（移除 R3/AC13/AC5b 待补）。

---

## 11. 变更记录

| 版本 | 日期       | 变更                                                                                                                                                                                                                                                                                                                                                                                                 |
| :--- | :--------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| r1   | 2026-09-23 | 首次成文：C-46…C-63；决策 D-1…D-6；三分表（删除 2 / 修订 7 / 保留 6，**逐条 15 行**）；威胁模型 + RS-1…RS-6；迁移最小充分集 + 门退役 5 步；被否 N-1…N-4；PRD 评审（§9）                                                                                                                                                                                                                              |
| r2   | 2026-09-23 | **A4 裁决落盘**：新增 **D-7/D-8**（web 接线 = ① 同构；账号切换 = 先清库）· 新增 **C-64/C-65/C-66** · **C-52 补强**（抽 `wipeUserData` 单一真源）· **C-60 补强**（截断不得谎报）· **§6 新增 N-5/N-6/N-7**（② 自建 pull / ③ 响应级缓存 / `id desc` 起拉）· **§7 状态更新**（U-1/U-2 已签核；U-3 已裁决）+ **§7.1 登记 Z17/Z18/Z19** · **§9.5 放行结论更新** · 新增 **§10（r2 增补）/ §11（变更记录）** |
| r3   | 2026-09-23 | **R3 裁定落盘 + AC 闭环确认**：D-5 措辞（不做密文化回滚路径）· **§4.5 删除「上线前回滚演练」要求**（改为前置验证 + 灰度）· **§9.4-R3 改为已裁定（接受不可逆）** · **U-4 改为已裁定** · **§9.5 移除 R3 阻塞项并重编号** · **§10.5 标为已闭环（AC13a/b/c + AC5b 已补齐，AC 共 24 行）** · 新增 **§10.6 R3 裁定** · 头部结论移除剩余阻塞项                                                              |